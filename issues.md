# Issues

| Date | Area | Description | Root cause | Status |
|------|------|-------------|------------|--------|
| 2026-04-27 | refresh.py | EPA Envirofacts SEMS tables (`SEMS_ACTIVE_SITES`, `SEMS_NPL`) return "table not available". | EPA data source path: switched to the EPA-hosted ArcGIS FeatureServer (`FAC_Superfund_Site_Boundaries_EPA_Public`) instead. | Fixed |
| 2026-04-27 | data | ~Some Superfund features carry `GIS_AREA_UNITS` of `Miles` (linear features) or `null`. | Source data quality. | Fixed — connector now keeps these with `acreage: null` (`--include-no-acreage` default on); frontend renders "N/A". |
| 2026-04-27 | frontend | Local preview blocked by sandbox (`scripts/serve.py` not permitted to run). | Sandbox permission. | Open — does not affect production; Playwright e2e tests cover golden path via subprocess `http.server`. |
| 2026-04-27 | data | EPA FeatureServer stores some sites (Portland Harbor: 100 fragments, Eielson AFB, several others) as multiple features sharing one EPA_ID. v1 rendered them as duplicate markers and undercounted acreage (Portland Harbor showed 4,577 ac instead of 9,488 ac). | Source data quality + missing aggregation. | Fixed — `SuperfundNPL._merge_by_epa_id()` collapses fragmented sites: rings concatenated, acreage summed when units match. Tested in `tests/test_merge_by_epa_id.py`. 2114 features → 1908 unique sites. |
| 2026-04-27 | data | NPL status codes `S` (Pre-proposal Site) and `W` (Withdrawn) appeared in source data but weren't in the hardcoded `NPL_STATUS_LABELS` dict. | Hardcoded labels drift from source. | Fixed — labels now pulled dynamically from layer's coded-value domain at refresh time; hardcoded dict kept as offline fallback. |
| 2026-04-27 | deploy | `gh` token lacks `workflow` OAuth scope — `.github/workflows/deploy.yml` and `refresh.yml` exist locally but are not committed/pushed. Pages currently deploys via legacy `main:/docs` source instead of the deploy Action; `refresh.py` cron is not running. | Auth scope. | Fixed — added a fresh `pranava0x0` gh account via `gh auth login -w -s workflow,repo,gist,read:org`, removed the stale `praparla` account (no longer valid), pushed both workflow files, and switched Pages source from legacy `main:/docs` to GitHub Actions via `gh api -X PUT /repos/.../pages -f build_type=workflow`. |
| 2026-04-27 | frontend | Site never opened in a real browser during the build (sandbox blocked local server, Pages live URL not visually inspected). Tabs, markers, and side-panel interactions are unverified. | Tooling. | Fixed — Playwright smoke test added (`tests/e2e/test_smoke.py`) covering page load, tab switch, marker click, table-row click, Esc close, and legend render. CI runs it on every PR via `.github/workflows/test.yml`. |
| 2026-04-27 | frontend | `app.js` had a `let map` (Leaflet) shadowed by `const map = el("tab-map")` inside `wireTabs()` — caught and fixed during the build, no test covers it. | Code bug. | Fixed — Playwright smoke test now exercises both tabs + marker interactions; would have caught this. |
| 2026-04-27 | frontend | `docs/serve.py` (local dev shim) ships to GitHub Pages because the deploy source is the entire `docs/` directory. It's never referenced from `index.html` so it's harmless dead weight, but violates "never ship dev files to production." | Build hygiene. | Fixed — moved to `scripts/serve.py` (chdirs to docs/ so still runs from repo root with `python scripts/serve.py`). |
| 2026-04-27 | refresh.py | Envirofacts ACRES tables (`BF_PROPERTY`, `BF_GRANT_RECIPIENT`, etc.) return either "table not available" or `'NoneType' object has no attribute 'get'` for `BF_REPORTING`. EPA appears to be migrating these tables out of Envirofacts. | Source path. | Fixed — switched to the EPA-hosted ArcGIS service `All ACRES Properties 8_30_2021` (36,003 records). ACRES dataset is from 2021 — see backlog item to refresh to the latest annual snapshot. |
| 2026-04-27 | data | Adding 36k ACRES records to a single `sites.json` would push gzipped first-paint payload from ~170KB to ~2MB — too heavy for mobile. | Scale. | Fixed — `refresh.py --all` writes per-source files (`superfund-npl.json`, `epa-acres.json`) and mirrors the canonical Superfund file to `sites.json`. The frontend lazy-loads `epa-acres.json` only when the user enables the Brownfields filter. First paint stays at 170KB gz; ACRES is a one-time 1.5MB gz fetch on demand. |
| 2026-04-27 | data | Some ACRES properties have lat/lon at (0, 0) — typical geocoder failure for "MULTIPLE ADDRESSES" rows. | Source data quality. | Fixed — `EpaAcres.normalize()` rejects coordinates within 0.5° of null island and out-of-range values (`abs(lat) > 90`, `abs(lon) > 180`). Tested in `tests/test_acres.py`. |
| 2026-04-27 | frontend | At 38k markers, drawing every point at zoom 4 is wasteful and degrades pan/zoom on mobile. | Perf. | Fixed — hash-based marker decimation: zoom ≤4 keeps 1/8, ≤5 keeps 1/4, ≤6 keeps 1/2, ≥7 shows all. Stable across zoom changes (same hash bucket = same subset). |
| 2026-04-27 | frontend | Status colors duplicated across `colorForStatus()`, `STATUS_LEGEND`, `--status-*` CSS vars, and `.pill[data-status]` rules. Theme swap would need four edits. | Drift. | Fixed — colors live in CSS vars only. JS reads via `getComputedStyle(html).getPropertyValue('--status-final')` etc., and dark-theme is a single `[data-theme="dark"]` block override. Markers re-stylize on theme swap. |
| 2026-04-27 | frontend (map) | CARTO basemap rendered Canada/Mexico landmasses, state borders were too faint to read at low zoom, and tile colors didn't track the dark/light theme. | UX gap — raster tiles can't be themed, and even with `bounds: US_BOUNDS` the tiles include neighboring countries. | Fixed — replaced CARTO tile layers with a vector basemap (CNN-election-tracker style): US states GeoJSON (`docs/data/us-states.json`, ~87KB) drawn as filled polygons with bold strokes, US counties TopoJSON (`docs/data/us-counties-topo.json`, ~822KB) lazy-decoded via topojson-client and rendered at zoom ≥ `COUNTY_MIN_ZOOM` (7). Land/ocean/border colors are CSS-var-driven and `refreshBasemapColors()` repaints on theme swap. Added `<meta name="color-scheme" content="light dark">` so production browsers don't auto-invert the canvas. |
| 2026-04-29 | frontend (map) | Real Alaska / Hawaii state polygons render at their true geographic location (top-left and far-bottom-left of the map viewport), while their markers are remapped into the bottom-of-map inset boxes via `applyInsetRemap()`. | Code bug — `applyInsetRemap()` only rewrites `SiteRecord.lat/lon`; the `us-states.json` GeoJSON layer renders unmodified. | Fixed 2026-04-29 — `drawBasemap()` filters out features whose `properties.name` is in `NON_CONUS_STATE_NAMES` (Alaska, Hawaii, Puerto Rico) before instantiating the GeoJSON layer. The inset boxes carry the visual representation for those states. |
| 2026-04-29 | frontend (perf) | Cold load freezes the renderer for 30+ seconds. All 36k ACRES markers added to the Leaflet canvas synchronously. | Code bug — synchronous marker hydration on the main thread. | Fixed 2026-04-29 — `hydrateMarkersChunked()` batches 800 markers per `requestIdleCallback` tick (falls back to `setTimeout(0)`), yielding to the main thread between batches. Cold-load DOMContentLoaded now fires in ~60 ms; markers light up progressively while the page stays interactive. |
| 2026-04-29 | docs | Project `CLAUDE.md` "Multi-source loading + lazy ACRES" note describes ACRES as lazy-loaded only when the Brownfields toggle flips, but in v1.3 both programs are on by default. | Doc drift. | Fixed 2026-04-29 — note rewritten to describe the auto-load + chunked-hydration pattern; opt-out via `?program=superfund` is the only path that skips the ACRES fetch. |
| 2026-04-29 | frontend (testability) | No programmatic ready-signal exposed by `app.js`. | Missing affordance. | Fixed 2026-04-29 — `markAppReady()` sets `window.__APP_READY__ = true` and dispatches `brownfield:ready` on `document` after the chunked ACRES hydration completes (or immediately when brownfields are off). The e2e suite uses this hook in place of polling header text. |
| 2026-04-29 | frontend (perf) | Table view renders all 37,911 sites as `<tr>` elements at once. ~265k DOM nodes. | Code bug — no virtualization / pagination. | Fixed 2026-04-29 — paginated at `TABLE_PAGE_SIZE = 250` with an IntersectionObserver-driven sentinel that auto-appends the next page when the user scrolls within 300px of the end. Total DOM nodes drop to ~2,700 (regression-tested in `test_dom_size_under_5k_nodes`). `applyFilter()` now rebuilds the paginated table from in-memory state; zoomend only re-runs marker decimation. |
| 2026-04-29 | frontend (detail panel) | `.detail[hidden]` had `display: block` overriding the `[hidden]` default of `display: none`, leaving a sliver of stale text on the right edge after closing. | CSS specificity. | Fixed 2026-04-29 — added `visibility: hidden` to `.detail[hidden]` with a transition delay matching the slide-out (180 ms), bumped translateX to 120% so subpixel rounding can't leak a sliver, and synced `aria-hidden` in JS. Regression test in `test_detail_panel_truly_hides_on_close`. |
| 2026-04-29 | frontend (URL state) | Filter-clear actions don't unwind URL params. | Code bug. | Fixed 2026-04-29 — verified by `test_url_unwinds_on_filter_clear`. The current `syncUrl()` already drops keys at default; the original report likely confused Escape (cleared) vs. losing focus (kept). Test now guards against future regressions. |
| 2026-04-29 | frontend (URL state) | Direct navigation to `?site=<EPA_ID>` with an unknown ID silently strips the param and opens an empty detail panel. | Code bug. | Fixed 2026-04-29 — `applyUrlSelection()` now waits for `acresLoadingPromise` if brownfields are loading, then either selects the site or shows a non-blocking toast (`showToast(\`Site \"${id}\" not found — check the EPA ID.\`)`). Toast styling lives in `.toast` / `.toast.visible`. |
| 2026-04-29 | frontend (map) | Filtering to a small set leaves the lower-48 view; matching markers are visually lost. | UX bug. | Fixed 2026-04-29 — `refitMapToFilters()` runs after each user filter change (search debounced 350 ms, slider debounced 350 ms, state/status/program immediate). Heuristics: skip when 0 or >5,000 visible (no narrow), zoom to 12 for a single result, otherwise `fitBounds` only when the visible bbox is <50 % of the current viewport area. |
| 2026-04-29 | frontend (map) | Counties don't render after auto-zoom via `?site=` / detail panel — `ensureCountiesLoaded()` was gated on `zoomend` only. | Code bug. | Fixed 2026-04-29 — also hook `moveend` so any view change (including `setView`-triggered transitions) re-evaluates `updateCountyVisibility()`. |
| 2026-04-29 | frontend (detail panel) | "N/A — see backlog" leaks dev jargon; "Code Not a Federal Facility" leaks raw EPA coded-value. | Copy bug. | Fixed 2026-04-29 — `index.html` now ships `Not available` (with `.muted-cell` styling) for the four placeholder rows. `selectSite()` decodes `s.federal_facility` directly (already a clean label from the connector) and collapses EPA's upstream double-space ("Not a  Federal Facility") to a single space. |
| 2026-04-29 | frontend (toolbar) | Search input too narrow; placeholder truncated. | UX/Layout. | Fixed 2026-04-29 — search input is now `flex: 1 1 240px; max-width: 360px`; placeholder shortened to "Search sites…" with the longer description in `aria-label` and `title`. The search-count was moved out of the search wrapper so a long count text doesn't compress the input. |
| 2026-04-29 | frontend (theme) | Light-theme state strokes too faint; inset boxes blend into background. | CSS-var palette. | Fixed 2026-04-29 — `--map-state-stroke` darkened from `#6b7689` to `#4a5568`. New `--inset-bg` / `--inset-stroke` tokens in both themes; `drawInsetBoxes()` reads them via `cssColor()`. |
| 2026-04-29 | frontend (filters) | State dropdown shows postal codes only; territories sorted alongside states. | UX. | Fixed 2026-04-29 — `populateStateFilter()` renders `<option>Alabama (AL)</option>`-style labels sorted by full state name, with territories in a dedicated `<optgroup label="Territories">`. `STATE_NAMES` and `TERRITORY_NAMES` are the source of truth. |
| 2026-04-29 | frontend (filters) | NPL Status filter uses `<select multiple>` (Cmd/Ctrl-click required, non-obvious). | UX. | Fixed 2026-04-29 — replaced with a fieldset of four checkboxes (`#f-status-checks input[data-status]`). Delegated change handler so `populateStatusFilter()` can rebuild without re-binding listeners. Reset button clears all checkboxes. |
| 2026-04-29 | frontend (a11y) | No skip-to-content link, no toolbar landmark, stale detail-panel headings in closed state. | A11y. | Fixed 2026-04-29 — added `<a class="skip-link" href="#main">`, wrapped the toolbar in `<nav aria-label="Toolbar">`, gave `<main>` an `id="main"` and `tabindex="-1"`. The detail panel `aside` carries `aria-hidden` synced to its `hidden` attr (true on close, false on open) and `role="complementary"`. |
| 2026-04-29 | frontend (data quality) | City column shows county names; ALL CAPS. | Data quality + UX. | Fixed 2026-04-30 — frontend prettifier (`prettyPlace()` in `app.js`) title-cases `s.city`, `s.county`, and `s.address` at ingest time. Connector-side fix avoided so existing data files don't need re-fetch; raw source preserved on `s.{city,county,address}_raw` for debugging. Sentinels (`-- Not Defined --`, `_NULL_`) collapse to `null`. Regression test in `test_city_and_county_titlecased`. |
| 2026-04-29 | frontend (perf) | At wide desktop widths, lower-48 fitBounds leaves nearly half the viewport empty. | UX. | Fixed 2026-04-30 — v1.8 hero strip + KPI deck fills the previously-empty top of the page with editorial copy + 4 headline metrics (`#kpi-total`, `#kpi-acres`, `#kpi-dc`, `#kpi-states`). Footer with sources + refresh date fills the bottom. Map area on a 27" monitor is now visually balanced rather than aspect-stretched. |
| 2026-04-29 | frontend (perf) | Tests caught the `wireTabs()` shadow bug 2026-04-27 but didn't catch `applyFilter()` rebuilding the table on every zoomend. | Code bug — coupled concerns. | Fixed 2026-04-29 — `applyFilter()` (full rebuild) is now distinct from `applyMarkerVisibility()` (zoom-only). zoomend handler calls only the latter. |
| 2026-04-30 | frontend (UAT-001) | Filter chip badge on the gear icon (`#filters-chip`) was always visible regardless of `hidden` attribute — `display: inline-flex` overrode the implicit `display: none` from `[hidden]`. Same class of bug the detail panel had in 2026-04-29. | CSS specificity. | Fixed 2026-04-30 — added explicit `.chip-count[hidden] { display: none }`. Regression tests `test_chip_count_hidden_attribute_works` + `test_filter_chip_hidden_by_default_visible_when_active`. CLAUDE.md updated with a note about the `[hidden]` trap so future code-paths add the rule alongside any `display: ...` override. |
| 2026-04-30 | frontend (UAT-002) | Detail panel address showed raw ALL CAPS source text (e.g. "FOX RIVER AND GREEN BAY, Green Bay, WI"). | Code bug — `s.city` was prettified at ingest but `s.address` was missed. | Fixed 2026-04-30 — `ingestSites()` now also runs `s.address` through `prettyPlace()`. Regression test `test_address_titlecased_in_detail_panel`. |
| 2026-04-30 | frontend (UAT-003) | KPI labels truncated on tablet (e.g. "ACRES TRACK...", "DC REUSE CAND...") because `.kpi-label` had `white-space: nowrap; text-overflow: ellipsis`. | Layout. | Fixed 2026-04-30 — `.kpi-label` allows wrap with `min-height: 2.4em` so 1- and 2-line labels align. Shortened "DC reuse candidates" subline and "States covered" label so the most-truncated cells fit comfortably. |
| 2026-04-30 | frontend (UAT-004) | Marker tooltip stayed open after closing the detail panel. | Code bug — `closeDetail()` cleared the table-row highlight but not the marker tooltip. | Fixed 2026-04-30 — `closeDetail()` now calls `markersById.get(selectedId)?.closeTooltip()` before nulling `selectedId`. |
| 2026-04-30 | frontend (UAT-005) | Topbar `#meta` text was hardcoded to "X Superfund + Y brownfields" template, so once FUDS or BRAC lazy-loaded the breakdown labels became wrong (the "Y brownfields" count actually included FUDS). | Code bug — string template assumed two programs. | Fixed 2026-04-30 — `updateMetaText()` derives the breakdown from per-program counts in the in-memory `sites` array. Regression test `test_meta_text_shows_per_program_counts` asserts all four program names appear. |
| 2026-05-03 | data (FUDS) | All ~10k FUDS records had `acreage: null` because the connector queried layer 1 (points-only). Severely hampered acreage-based filtering and the data-center thesis (≥50ac threshold). | Wrong source layer. | Fixed 2026-05-03 (v1.9) — `connectors/dod_fuds.py` now joins layer 1 + layer 4 (`FUDS Property Polygon`) by `DODFUDSPROPERTYIDPK`. Acreage computed via Shoelace + cos(latitude) (`connectors/geom.polygon_acreage`). ~3k previously-null records gained acreage. Same property's parcel-fragment polygons get rings concatenated before area calc so we sum across fragments. Tested in `tests/test_fuds.py:test_polygon_join_aggregates_multi_parcel_acreage`. |
| 2026-05-03 | data (Superfund) | No related-document data per site. The detail panel had no way to surface RODs, ESDs, Five Year Reviews, or fact sheets — users had to hunt for the EPA pretty page manually. | Missing source. | Fixed 2026-05-03 (v1.9) — new `connectors/epa_superfund_docs.py` enrichment connector. Three-hop walk (EPA pretty page → SF_SITE_ID → cumulis docdata → SEMS cachejson) lands per-site `documents: [{title, url, date, category, ...}]` arrays. Resumable batched coverage (`--docs-limit 100 --docs-skip M`). Detail-panel "Federal documents" block renders the top N with deep-link to canonical SEMS for full coverage. |
| 2026-05-03 | research | Backlog claimed the EPA ACRES Property Profile (PPF) URL `acres6.epa.gov/acres/cms/PropertyProfileReports/Output/<ID>.html` was scrape-able for acreage + owner. Live re-test 2026-05-03 returned a 302 to `wamssoprd.epa.gov/oam/...` — login-only behind EPA WAM SSO. | Source-access drift (or backlog was wrong from the start). | Fixed 2026-05-03 (v1.9) — backlog updated to mark scraping-PPF as a dead path. Two viable paths remain: bulk extract via helpdesk@acrebs.epa.gov, or commercial parcel-API fallback (Regrid / Landgrid). |
| 2026-05-03 | research | Initial Superfund-docs implementation assumed the cumulis profile URL `csitinfo.cfm?id=<X>` would accept `EPA_ID`. It doesn't — only `SF_SITE_ID` works, and the two are unrelated identifiers with no public cross-walk dataset. First test run returned 0 docs across 5 sites despite being valid Final NPL records. | Wrong identifier assumption. | Fixed 2026-05-03 (v1.9) — added a third hop to the connector: scrape the EPA pretty page (`https://www.epa.gov/superfund/<slug>` from `URL_ALIAS_TXT`) and regex-extract `csitinfo.cfm?id=(\d+)` to get the SF_SITE_ID. Cached aggressively because the mapping never changes per site. Sites without a pretty URL are skipped (none in current dataset). Documented in CLAUDE.md so future hops know to use the pretty-page hop, not assume EPA_ID. |
| 2026-05-03 | frontend (UAT-006) | KPI deck subtext bleeds across cell boundary at desktop widths. At 1280px viewport, "4.9K sites with reported area" (~146px wide) overflows the ~125px ACRES TRACKED cell by ~33px and visually collides with the DC Reuse Candidates subtext "≥50 ac · power · water" — produces a smashed-together "reported a≥50 ac · power · water" reading on the page. | CSS bug — `.kpi-sub` is a `<span>` rendered with `display: inline`, so the `overflow: hidden` and `text-overflow: ellipsis` rules silently no-op (those properties only apply to block-level / inline-block / table-cell elements). | Fixed 2026-05-03 — `.kpi-sub` set to `display: block` so overflow + ellipsis activate; added `min-width: 0` to `.kpi` to let grid items shrink below their intrinsic content width. Mobile carousel (`<640px`) keeps `white-space: normal` for two-line wrapping. Regression test `test_kpi_subtext_does_not_overflow_cell` asserts each `.kpi-sub`'s `getBoundingClientRect().right <= cell.right` at desktop width. |
| 2026-05-03 | frontend (UAT-007) | Reset button (gear → Reset) silently restores stale v1.6 defaults. After toggling any program filter and clicking Reset, only `superfund` + `brownfield` are re-added to `filterState.programs`; `fuds` and `brac` stay excluded. Symptoms: chip badge keeps showing "1", URL keeps `?program=superfund,brownfield`, search-count drops from 46,759 to 37,911 (FUDS+BRAC vanish), and the Reset action does nothing visible because the DOM checkboxes for fuds/brac were already checked from before the click. | Code regression from v1.7 — when FUDS + BRAC programs were added to `PROGRAM_LEGEND`, the Reset handler at `app.js:1127` was never updated. It still hard-codes `new Set(["superfund", "brownfield"])` and only re-checks `progBoxes.superfund` / `progBoxes.brownfield`. | Fixed 2026-05-03 — Reset handler now restores all programs from `PROGRAM_LEGEND` (`new Set(PROGRAM_LEGEND.map(p => p.program))`) and re-checks every box in `progBoxes`. Triggers `ensureFudsLoaded()` / `ensureBracLoaded()` if they hadn't loaded yet (e.g. user arrived via `?program=superfund,brownfield`). Regression tests `test_reset_restores_all_four_programs` and `test_reset_clears_filter_chip` guard against future drift. CLAUDE.md updated with a one-line note that the Reset handler must iterate `PROGRAM_LEGEND` rather than hardcoding program slugs — same drift risk we already documented for `populateStatusFilter()`. |
| 2026-05-04 | refresh.py (ordering) | `epa-superfund-docs` reads `docs/data/superfund-npl.json` but in `--all` mode connectors run alphabetically — `epa-superfund-docs` (e) sorts before `superfund-npl` (s), so on cold runs the docs connector saw zero sites and silently produced an empty enrichment. Latent since v1.9; only worked because subsequent re-runs found the prior `superfund-npl.json` from disk. | Code bug — alphabetical-only ordering doesn't honor enrichment dependencies. | Fixed 2026-05-04 (v1.10) — added `Connector.run_order: int = 100` (default) and bumped `epa-superfund-docs` and `infra-proximity` to 200/300. `refresh.py --all` now sorts by `(run_order, slug)` so producers always run before enrichments. Regression test `test_run_order_last_in_all` guards against future drift. |
| 2026-05-04 | refresh.py (TIGERweb) | TIGERweb `Transportation/MapServer/2/query` (Primary Roads) advertises `maxRecordCount: 100000`, but pages of 10,000 features with `returnGeometry=true` return HTTP 500 ("Error performing query operation"). Discovered while building the v1.10 infra-proximity connector. | Source-data quirk — advertised max is for non-geometry queries; geometry payload limit is much smaller. | Fixed 2026-05-04 (v1.10) — `LAYERS["highway"]["page_size"]` and `LAYERS["rail"]["page_size"]` set to 1000 in `connectors/infra_proximity.py`. CLAUDE.md note added so future TIGER connectors don't repeat the discovery. |
| 2026-05-04 | research (HIFLD substations) | The canonical HIFLD `Electric Substations` national endpoint that pairs with `Electric_Power_Transmission_Lines` on `services1.arcgis.com/Hp6G80Pky0om7QvQ` does not appear to exist on that host. The closest national-named endpoint at `services.arcgis.com/G4S1dGvn7PIgYd6Y/.../HIFLD_electric_power_substations` has only 128 features (NJ/PA-only extent). Other "HIFLD Electric Substations" endpoints in ArcGIS search either return empty metadata or are auth-walled. | Source-access drift — HIFLD has migrated services without a single canonical replacement for substations. | Open — v1.10 ships transmission, rail, and highway layers; substation field deferred to v1.11. Backlog item updated. Likely path: OpenStreetMap `power=substation` extracts via OpenInfraMap. |
| 2026-05-04 | data (Superfund) | No enforcement / compliance signal per site. Open ECHO enforcement, recent violations, or unpaid penalties make a site materially untransactable, but the dashboard had no surface for that data — buyers had to cross-check ECHO manually with the EPA_ID. | Missing source. | Fixed 2026-05-04 (v1.11) — new `connectors/epa_echo.py` enrichment connector. One HTTP call per site to `echodata.epa.gov/echo/echo_rest_services.get_facilities?p_si=<EPA_ID>` returns the headline ECHO summary (5yr inspections, formal/informal actions, penalties, last violation date, current compliance, active programs). Detail-panel "Enforcement & compliance" block renders the fields with red-highlighted styling for nonzero formal actions and nonzero penalties. Resumable batched coverage (`--echo-limit 100 --echo-skip M`). Deep-link to the canonical Detailed Facility Report on echo.epa.gov. |
| 2026-05-04 | UX (synthesis gap) | Even with every structured field in the panel — name, status, acreage, infra distances, documents, enforcement — buyers still have to mentally synthesize "is this site worth a closer look?". No federal or commercial competitor synthesizes the data into prose. | Missing affordance. | Fixed 2026-05-04 (v1.11) — new `connectors/ai_summary.py` enrichment connector calls Claude Haiku (`claude-haiku-4-5-20251001`) to generate a 3-paragraph narrative per site (what it is / reuse signals / material risks). Cached by content-hash so re-runs only re-bill when the underlying fields actually change. Surfaced as a new "Summary" tab in the detail panel. `--dry-run` works without an API key (cache-only re-build). |
| 2026-05-04 | tests (over-specification) | `test_run_order_last_in_all` asserted `infra-proximity` was the literal last connector in `--all` order. Adding the v1.11 `ai-summary` connector at `run_order=400` broke the test even though the actual contract (infra-proximity runs after every producer) was preserved. | Test bug — assertion was over-specific to the v1.10 connector lineup. | Fixed 2026-05-04 (v1.11) — renamed to `test_run_order_after_all_producers`; now iterates `connectors_pkg.names()` and asserts `infra-proximity.run_order > cls.run_order` for every connector at the producer tier (run_order=100). Future enrichment connectors at any run_order won't trip it. |
