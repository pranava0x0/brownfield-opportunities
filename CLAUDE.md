# CLAUDE.md — Universal Development Principles

> Distilled from patterns across multiple projects. Apply universally; skip sections irrelevant to the current project type.

---

## Agent Workflow: Explore → Plan → Code → Verify

Never blindly write code. Always follow this loop:

1. **Explore** — Search the codebase. Find relevant files, understand existing patterns before touching anything.
2. **Plan** — Assess the blast radius (how many files touched, how long it takes). For significant changes, present 2–3 high-level approaches with pros/cons and ask for human approval before writing code.
3. **Code** — Implement following the rules below.
4. **Verify** — Run tests. Fix all failures before declaring the task complete.

**Read before edit:** Always read a file before editing it, even if it was read earlier in the conversation.

**Ask for options first.** On non-trivial tasks, propose approaches before writing code. The human needs to evaluate options — don't assume the first plausible approach is the right one.

---

## Communication Style

- **Concise output.** No filler, no apologies, no moralizing. Skip generic advice.
- **Show your work.** Use short internal monologues to break down complex problems.
- **Fail loud.** Never use catch-all exception handlers that silently swallow errors. Always raise or log explicitly.

---

## Architecture Principles

- **No over-engineering.** Only make changes directly requested or clearly necessary. Keep solutions simple.
- **Single source of truth.** Constants, configs, and shared types derive from one place.
- **Modular design.** Separate concerns: data fetching, processing, storage, and presentation are distinct layers.
- **Idempotent operations.** Re-running any operation should be safe and produce the same result. Use `INSERT OR IGNORE` patterns, cache checks, or deduplication by unique key.
- **Static when possible.** Prefer baked-in data over runtime backends when the data update cycle allows it.
- **Cost-optimized.** Stay on free tiers and use the cheapest resources that meet requirements.
- **CLI-first.** Build CLI entry points before UI. Agents can invoke CLIs directly to self-validate output, closing the feedback loop without human intervention.
- **Minimize page weight and request count.** Audit total payload size and number of requests. Content-focused sites should be lightweight — aim for fewest requests and smallest payload possible.
- **Tree-shake and code-split.** Don't bundle every controller/feature for every page. Use code-splitting and lazy loading so pages only load the code they actually need.
- **Benchmark against best-in-class.** Compare your site/app against well-optimized reference points. If the simplest site in your org is orders of magnitude lighter, your build process needs review.
- **Document subsystems.** Maintain a `docs/` folder with notes on non-obvious subsystems, design decisions, and correct CLI invocations. One line of documentation prevents repeated mistakes.

---

## Error Resilience

- **Never let a single item failure crash the pipeline.** Wrap individual record processing in try/except. Log and continue.
- **Log aggressively.** Every request, parse, API call, cache hit/miss, and filter decision should be logged.
- **Cache everything.** Re-runs should be fast and cheap. Multi-layer caching where appropriate.
- **Validate everything.** Invalid responses from external services → log and skip, never crash.
- **Track errors visibly.** Use an `issues.md` file or errors array — failures must be visible, not silent.

---

## Security & Credential Handling

- **Never commit secrets.** API keys, tokens, and passwords must never appear in committed code.
- Read credentials from environment variables only (e.g., `os.environ["API_KEY"]`). Halt with a clear error if missing.
- Never log or print credential values.
- Always `.gitignore`: `.env`, `.env.local`, `credentials.json`, `secrets/`, `node_modules/`, `__pycache__/`, `dist/`, `*.pyc`.
- Before committing: `git diff --cached | grep -iE "apikey|password|token|secret"`.
- **Respect user privacy choices.** Don't circumvent ad blockers or privacy tools by proxying tracking SDKs through your own domain. This erodes user trust.

---

## Testing & Validation

- **Write tests alongside code, not as an afterthought.** Every new module or bug fix includes corresponding tests.
- Write a regression test for every bug fix.
- Validate output data against expected schemas before writing to disk.
- **Cover edge cases, not just happy paths:**
  - Empty input: `[]`, `{}`, `""`
  - Null/undefined for every optional field
  - Boundary values (first/last page, exact date boundaries, zero counts)
  - Combined states (e.g., multiple filters active simultaneously)
- Run the full test suite before committing to catch regressions.
- **Never ship test files to production.** Ensure build pipelines exclude test files, dev fixtures, and debug artifacts from production bundles. Use build exclusions and CI checks to enforce this.

---

## Git Discipline

- **Commit often** at natural checkpoints — small, focused commits over large monolithic ones.
  - After each new module/feature is built
  - After fixing a bug or resolving a failing test
  - After updating documentation
- Write descriptive commit messages explaining *what* and *why*.
- Never commit large binary files, downloaded data, or API keys.

---

## Data Handling

- **Append-only data.** Append new records rather than overwriting. Deduplicate via unique keys.
- **Source attribution.** Every data record must include its origin (source URL, connector name, etc.). Users must be able to trace data back to its source.
- **Defensive optional field handling.** Null-check every optional field before rendering or processing.
- Null values show explicit placeholders ("N/A", "TBD", "Value TBD") — never blank UI elements or missing fields.

---

## Issue Tracking (`issues.md`)

Maintain a living `issues.md` in the project root as an audit trail.

- Log bugs with: date, module/area, description, root cause (**code bug** vs. **test bug**), and status (Open / Fixed).
- Update entries when resolved: what the fix was + the commit that resolved it.
- After every bug fix, check whether a new regression test is needed.

---

## Backlog (`backlog.md`)

Maintain a `backlog.md` for ideas, features, and enhancements.

- When ideas come up during development, add them immediately — don't lose them.
- Each item: brief description + priority (low / medium / high).
- Review and reprioritize periodically.

---

## Python Standards

*(Apply when the project uses Python)*

- Type hints on all functions.
- Use `pathlib.Path` for file paths.
- Use the `logging` module — no bare `print` for runtime output.
- All constants in a single config module.
- Pin dependencies in `requirements.txt`.
- Use Pydantic for data validation.
- Python 3.9+ compatible unless specified otherwise.

---

## Frontend Standards

*(Apply when the project has a web frontend)*

- Functional components + hooks only. No class components.
- Colors, enums, and constants in a dedicated constants file — never hardcoded inline.
- Data transforms belong in hooks or utility functions, not in components.
- Proper loading, error, and empty states on every view.
- All interactive elements must have visible focus indicators for accessibility.
- **Mobile-first responsive design.** All features must work on both mobile and desktop.
- Use TypeScript strict mode when the project uses TypeScript. No `any` types.
- **Deduplicate image assets.** Serve each image exactly once. Use `<picture>` with `srcset` so the browser selects the best format (AVIF > WebP > PNG) rather than downloading all variants.
- **Serve optimized image formats.** Always use an image CDN or optimization pipeline. Never serve uncompressed PNGs for content images in production.
- **Only load libraries used on the page.** Don't let backend-only dependencies leak into read-only frontend pages.
- **Write descriptive `alt` attributes.** Every content image needs meaningful alt text for accessibility — never leave `alt=""`.
- **Use responsive CSS, not duplicate DOM trees.** Handle mobile/desktop layouts with CSS media queries — never render the same content twice in the DOM.

---

## Network Ethics & Rate Limiting

*(Apply when the project fetches from external sources)*

- Minimum 1.5–2s delay between requests to any single host.
- Set an informative `User-Agent` header.
- Handle 429 responses with exponential backoff (start at 10s).
- Cache all fetched content to disk. Re-runs should never re-download already-cached content.
- If a service persistently blocks after retries, log to `issues.md` and gracefully skip. Never crash.
- Start small when testing scrapers — validate against a handful of pages before scaling to full runs.
- **Use an image CDN or optimization pipeline.** Never serve raw, uncompressed images directly from object storage. Compress and convert to modern formats (WebP/AVIF) before delivery.

---

## AI/API Cost Optimization

*(Apply when the project uses LLM APIs)*

- Use the cheapest model that meets quality requirements by default (e.g., Haiku before Opus).
- Apply keyword pre-filtering to skip irrelevant content before sending to expensive APIs.
- Truncate/excerpt input text to reduce token usage.
- Cache API responses by content hash. Never re-classify identical content.
- Log cost impact at each optimization layer. Print a cost summary at the end of each run.
- `--dry-run` and `--fetch-only` modes must work without an API key.

---

## Working with AI Agents

*Meta-principles for getting the most out of AI-assisted development.*

- **Context engineering over prompt engineering.** Fill the context window with exactly what's needed — no more, no less. Watch for three failure modes: *context poisoning* (early errors that compound), *context distraction* (irrelevant content that buries what matters), and *context clash* (contradictory instructions).
- **Start fresh on topic switches.** Use `/clear` when moving to an unrelated problem. Long mixed-topic contexts degrade quality. Break complex tasks into small steps and commit between them.
- **AI has no taste.** Actively review output for: excessive try/catch blocks, unnecessary abstractions, code bloat instead of refactoring, and poor judgment on simplicity vs. structure. These are recurring failure modes that require human correction.
- **AI is a tool, not a substitute for engineering discipline.** Always apply fundamentals to AI-generated code: performance auditing, bundle analysis, code review, and optimization passes. High LOC output is meaningless if the code is bloated, duplicated, and unoptimized. Shipping fast doesn't mean shipping well.
- **Closed-loop validation.** Build projects so the agent can compile, lint, run tests, and verify its own output without human intervention. When the agent can close the loop itself, you can trust the result.
- **Keep this file current.** When something unexpected happens — a pattern that failed, a correct CLI invocation, a library quirk — add a concise note here. This file should grow incrementally as organizational scar tissue, not be rewritten from scratch.
- **Write big plans to files.** For large tasks, write the spec to a `docs/` markdown file and review it before executing. This persists context across sessions and allows a second-opinion review before building.

---

## Project-specific notes (Brownfield Opportunities)

- **Connector framework.** All data sources live under `connectors/` and inherit from `connectors.base.Connector`. The connector owns its CLI flags (via `add_cli_args`), its cache key namespace (filename prefix = `slug`), and its normalize logic. The driver (`refresh.py`) handles HTTP infra, schema validation, and output write. To add a source: drop one file in `connectors/`, register it in `connectors/__init__.py`. **Do not** put fetch logic in `refresh.py` itself — it's intentionally thin. Every record MUST set `id` (cross-source unique — Superfund uses raw EPA_ID, ACRES uses `ACRES-<PROPERTY_ID>`, FUDS uses `FUDS-<PROPERTY_ID>`, BRAC uses `BRAC-<SITE_NAME_SLUG>`) and `program` (`"superfund"` | `"brownfield"` | `"fuds"` | `"brac"`). As of v1.7 there are 5 connectors: `superfund-npl`, `epa-acres`, `dod-fuds`, `dod-brac`, `epa-redev` (enrichment-only, joins onto Superfund records).
- **Multi-source loading + lazy program data (v1.7).** `docs/data/sites.json` stays Superfund-only (~170KB gz) for fast first paint. Other programs lazy-load via `ensure*Loaded()` functions that de-dupe parallel fetches via shared promises: `epa-acres.json` (~36k brownfields, 1.5MB gz), `dod-fuds.json` (~8.8k FUDS, ~4MB), `dod-brac.json` (27 BRAC sites, 6KB), `epa-redev.json` (1,905 enrichment records, ~1.2MB). All four default-on; skip any via `?program=superfund` etc. `epa-redev` is special: it enriches existing Superfund records with infrastructure-proximity fields and `data_center_reuse_candidate` rather than adding new sites. All lazy loads fire via `Promise.allSettled()` after first paint; `markAppReady()` triggers after all resolve.
- **Chunked marker hydration (v1.6).** Adding 36k Leaflet circle markers synchronously froze the main thread for 30+s. `ensureAcresLoaded()` now hands the records to `hydrateMarkersChunked()` which calls `addOneMarker()` for 800 records per `requestIdleCallback` tick (falls back to `setTimeout(0)`); the returned promise resolves after the last batch lands. That's also when `markAppReady()` flips `window.__APP_READY__ = true` and dispatches `brownfield:ready` on `document`. Use those signals from automation; don't poll `epa-acres.json` 200 or `#meta` text.
- **Paginated table (v1.6).** Rendering all ~38k `<tr>` elements into the DOM took the doc to ~265k nodes and tanked DOM-interactive. The table is now paginated at `TABLE_PAGE_SIZE = 250`: `tableState.sorted` / `tableState.filtered` live in memory, only the first N filtered rows are in the DOM, and an IntersectionObserver-driven sentinel (`.table-sentinel`) auto-appends the next page within 300px of the scroll bottom. Filter / sort changes call `rebuildTable()` → `refreshTableForFilter()` (full reset to page 1). `zoomend` calls **only** `applyMarkerVisibility()` — never `applyFilter()` — because zoom changes don't affect the filtered set, only marker decimation. When a marker for a row past the rendered window is selected, `ensureRowRendered(id)` pages rows in until that ID lands so the highlight works. Total DOM nodes ~2,700 — regression-tested in `test_dom_size_under_5k_nodes`.
- **JSON serialization.** `Payload.model_dump_json(exclude_none=True)` is critical — without it every ACRES record carries 5+ explicit `null` fields (current_owner, encumbrances, etc.) that bloat the file ~30%. Default is also minified; `--pretty` overrides for diffing.
- **Color tokens are CSS-var-driven.** Two palettes live on `:root` (with `[data-theme="dark"]` overrides):
  - `--status-{final,deleted,proposed,other}` — drive the **NPL status pills only** (table column + detail panel).
  - `--program-superfund` / `--program-brownfield` / `--program-fuds` / `--program-brac` — drive **map markers, legend, and detail-panel top stripe** (set inline as `--detail-stripe` by `selectSite()`). Markers are colored by program (one dot per program), not by NPL status. JS reads via `getComputedStyle(html).getPropertyValue(varName)`. `colorForRecord()` reads `PROGRAM_COLOR_BY_PROGRAM`. **Don't** hard-code marker colors or duplicate the values in CSS — the legend, pill rules, and JS all read the same vars. On theme swap, `refreshMarkerColors()` and `rerenderLegend()` re-paint everything. v1.8 palette: Superfund = `#b8332f` light / `#ef5050` dark, Brownfield = `#1c5e9e` / `#7eb6e8`, FUDS = `#6f3691` / `#b47ad8`, BRAC = `#c46a14` / `#e6a040`.
- **Editorial design system (v1.8).** Hero strip with eyebrow + serif H2 + dek between topbar and filters; KPI deck (`#kpi-total`, `#kpi-acres`, `#kpi-dc`, `#kpi-states`) computed from the in-memory `sites` array (no extra fetches). Below the map, a `<footer class="site-footer">` cites the five data sources. Typography uses a system-serif display stack (`--font-serif: "Charter", "Source Serif 4", ...`) — **don't** add a Google Fonts link, the system stack is intentional for perf and offline-friendliness. KPIs and any new numerals get `font-variant-numeric: tabular-nums`. Hero copy is hidden on mobile (<640px) so the map keeps real estate; KPI deck becomes a horizontal scroll-snap carousel with two-line subtext wrapping. Tablet (<1024px) collapses the two-column hero (copy + KPI deck) to one column. **Don't** introduce backdrop-blur on the map legend or any other map-overlay element — it recomposites every pan/zoom frame and tanks mobile perf.
- **The `[hidden]` trap.** This codebase has been bit twice (detail panel 2026-04-29; filter chip 2026-04-30) by writing `display: inline-flex` / `display: block` on an element that also uses the `hidden` HTML attribute. The CSS rule wins, the attribute is ignored, and the element renders. **Always** add an explicit `[hidden] { display: none }` rule alongside any `display: ...` override. The detail panel takes the slow path (visibility transition); the chip takes the fast path (`display: none`).
- **Place-name prettifier (v1.8).** EPA / USACE source data ships cities, counties, and addresses ALL CAPS or with placeholder sentinels (`-- Not Defined --`, `_NULL_`). `prettyPlace()` in `app.js` runs in `ingestSites()` and overwrites `s.city`, `s.county`, `s.address`. Raw source preserved on `s.{city,county,address}_raw` for debugging. Sentinels collapse to `null`.
- **Site-name prettifier (v1.11.1).** Companion to `prettyPlace()` — `prettyName()` title-cases `s.name` at ingest and overwrites the raw value, with the original preserved on `s.name_raw`. The acronym whitelist (`NAME_KEEP_UPPER`) is the load-bearing piece: it's seeded from a frequency scan of the actual dataset (NIKE, AFB, AAF, AFS, NAS, NRDA, PCB, USDOE, USACE, USDA, USFS, BLM, NPS, USA, US, II/III/IV/…). When a new acronym shows up in a future data refresh that the list doesn't catch, add it to the set — every record on next ingest picks up the change automatically. **Don't** prettify names connector-side without a per-source whitelist — Superfund EPA-name conventions, FUDS USACE-name conventions, and BRAC installation-name conventions are different enough that one global list would mangle ~5–10% of the corpus. Frontend ingest is the right layer for now; promote to connector-side only when a non-frontend consumer materializes.
- **Meta text (v1.8).** `updateMetaText()` derives the topbar subtitle from per-program counts in `sites`. **Don't** hardcode "X Superfund + Y brownfields" templates — they break the moment FUDS or BRAC also lazy-loads (the previous bug had FUDS counts incorrectly labeled "brownfields"). Call `updateMetaText()` after every data load.
- **Filter chip (v1.8).** `#filters-chip` shows a small badge on the gear button with the count of active filters. `updateFilterChip()` is called from `applyFilter()`. The chip uses `[hidden]` + `display: none` (see "[hidden] trap" above) to fully remove it from layout when no filters are active.
- **Reset handler must iterate `PROGRAM_LEGEND` (v1.9.1, UAT-007).** The "Reset" button in the filters strip resets `filterState.programs` and re-checks every program box from `PROGRAM_LEGEND.map(p => p.program)` — **don't** hardcode `["superfund", "brownfield"]` or any other subset. When v1.7 added FUDS + BRAC, the reset handler stayed at the v1.6 two-program list and silently dropped FUDS/BRAC on every Reset click; the chip stayed lit and `?program=...` hung around in the URL. Same drift risk as `populateStatusFilter()` — anything that enumerates programs MUST iterate the legend so future programs pick up the change for free. Reset also has to call `ensureFudsLoaded()` / `ensureBracLoaded()` because the user may have arrived via a URL that skipped those programs (`?program=superfund,brownfield`).
- **`text-overflow: ellipsis` needs a non-inline display (v1.9.1, UAT-006).** `.kpi-sub` was a `<span>` — i.e. `display: inline` by default. `overflow: hidden` and `text-overflow: ellipsis` silently no-op on inline boxes; the subtext renders past the cell boundary into the next column instead of clipping with an ellipsis. Always set `display: block` (or `inline-block` / `flex` / `grid`) on any element where you rely on `text-overflow: ellipsis`. Combined with `min-width: 0` on the grid item parent so the column can actually shrink below the intrinsic content width.
- **Marker decimation.** With ~47k markers (1.9k Superfund + 36k ACRES + 8.8k FUDS + 27 BRAC), drawing every point at zoom 4 destroys mobile pan/zoom. `decimateKeep()` keeps 1/8 at zoom ≤4, 1/4 at ≤5, 1/2 at ≤6, all at ≥7. Stable hash-based (`shouldDecimateOut(id, keepEvery)`) so the visible subset doesn't reshuffle on zoom changes. Re-evaluated on `zoomend` via `applyMarkerVisibility()` (cheap — no table rebuild).
- **Auto-fit on filter narrow (v1.6).** `refitMapToFilters()` runs after each user filter change. Heuristics: skip when the visible set is empty or >5k (no meaningful narrow), zoom to 12 for a single result, otherwise `fitBounds` only when the visible bbox is <50 % of the current viewport area. Search and acreage are debounced 350ms so dragging / typing doesn't fitBounds every frame. Reset re-runs `fitBounds(US_BOUNDS)`. Don't call this from `zoomend` — the user's manual zoom would fight itself.
- **Non-CONUS state polygon mask (v1.6).** `drawBasemap()` filters `us-states.json` features whose `properties.name` is in `NON_CONUS_STATE_NAMES` (Alaska, Hawaii, Puerto Rico) before passing to `L.geoJSON()`. The matching markers are remapped via `applyInsetRemap()` into the bottom-of-map inset boxes, so dropping the real-world polygons keeps the visual story consistent. If a future `us-states.json` swap adds new territories with real-world polygons inside `US_BOUNDS`, add their `properties.name` to that set.
- **Toast helper (v1.6).** `showToast(message, { ms })` mounts a `#toast` div lazily, fades it in via `.toast.visible`, and auto-fades after 4s. Used today for "site not found"; reuse for any future non-blocking status messages. Don't grow this into a queue — if you need stacked toasts, swap in a real library.
- **URL state sharing.** `?site=<ID>` (legacy `?epa_id=` accepted), `?q=`, `?state=`, `?status=F,P`, `?program=superfund,brownfield,fuds,brac`, `?min_ac=<log10>`. Writes are debounced 200ms via `history.replaceState` so slider drags don't spam history. Programs only encoded when not the default (all four programs on).
- **Table STATUS column is NPL-specific; don't fall back to PROGRAM (v1.11.2, UAT-009).** `makeRow()` in `app.js` renders the STATUS cell. For Superfund, render the NPL pill (`s.npl_status` keyed by `s.npl_status_code`). For FUDS, surface `s.eligibility` as text — Eligible / Ineligible / Categorical Exclusion / No Further Action are the most actionable cleanup signals USACE provides. For ACRES + BRAC, render `<span class="muted-cell">—</span>`. **Don't** fall back to a `data-program` pill — that's how the bug shipped: every FUDS / BRAC / ACRES row showed an identical pill in PROGRAM and STATUS, doubling visual noise without adding signal. Test guard: `test_table_status_column_no_program_duplication` sorts by state and asserts no row has the same text in both columns. The `#sites-table td .muted-cell` style scope was added so the table picks up the same italic-muted treatment used in the detail panel's `.kv dd`.
- **`applyUrlSelection()` must wait on every program-data promise (v1.11.1).** Direct navigation to `?site=<ID>` runs through `applyUrlSelection()`. The function checks `sitesById.has(id)` first, and if not found it `Promise.allSettled`s every in-flight program-data fetch (`acresLoadingPromise` / `fudsLoadingPromise` / `bracLoadingPromise`) before declaring the ID unknown. **Don't** revert to checking just one promise — the v1.7 regression (only ACRES was checked) silently broke `?site=FUDS-XXX` and `?site=BRAC-XXX` for the entire lazy-load window. Same drift risk as the Reset handler (UAT-007) and `populateStatusFilter()` — anything that enumerates programs has to cover all of them. Boot order also matters: the lazy loads must be kicked off **before** `applyUrlSelection()` runs so the promises are populated; otherwise the wait branch sees `null`s and falls through to the toast immediately.
- **Schema is the contract.** `schema.py` (Pydantic) defines `SiteRecord` + `Payload` with `extra="forbid"`. Any connector that emits an unknown field fails before write. When adding fields, update the schema and the test fixtures together.
- **Multi-polygon merge.** EPA's FeatureServer stores some sites (e.g. Portland Harbor — 100 fragments) as separate features sharing one EPA_ID. `SuperfundNPL._merge_by_epa_id()` collapses them: rings concatenated, acreage summed when units match, first non-null wins for other attrs. Without this you get duplicate markers and undercounted acreage. Tested in `tests/test_merge_by_epa_id.py`.
- **Status-A sub-site dedupe.** NPL status `A` ("Site is Part of NPL Site") rows whose name starts with a non-A site's name are dropped from the main list and tagged with `parent_epa_id`. Orphan A sites (no name-prefix parent in dataset) are kept so we never silently lose data.
- **Coded-value labels are dynamic.** `NPL_STATUS_CODE` and `FEDERAL_FACILITY_DETER_CODE` labels are pulled from the layer's `?f=json` metadata at refresh time. The hardcoded `NPL_STATUS_LABELS` dict is a fallback for offline / outage scenarios only — when EPA adds a new code, the dynamic fetch picks it up automatically.
- **Diff log.** `diff.py` writes `data/changes.md` after each refresh. `refresh.yml` parses the summary line into the commit message. Only writes when the canonical `docs/data/sites.json` changed (avoids commit-per-cron-tick from changing timestamp alone).
- **No-acreage bucket.** Sites with `Miles` or null `GIS_AREA_UNITS` are kept with `acreage: null` — frontend shows "N/A" rather than dropping them. Toggleable via `--no-include-no-acreage` if you want the legacy behavior.
- **Tests are the safety net.** 185 unit tests + 41 e2e (v1.10). pytest unit tests cover normalize/envelope/fetch/dedupe/merge/diff/schema for all connectors (superfund-npl, epa-acres, dod-fuds, dod-brac, epa-redev, epa-superfund-docs, infra-proximity) plus shared `connectors.geom` polygon math and `connectors.spatial` segment-index math. Playwright e2e tests cover page load, tab switch, marker click, table click, Esc close, search filtering, legend render, all four programs loading, NPL status checkboxes, state dropdown, acreage slider, pagination, DOM size cap, accessibility, hero/KPI/footer rendering, meta text per-program counts, filter chip visibility, prettifier output, detail-panel program stripe, sort glyph, the `[hidden]` trap regression, plus v1.9: owner-source citation label and federal-documents block rendering, plus v1.9.1: KPI subtext clip + Reset button restoring all four programs + Reset clears filter chip, plus v1.10: cross-program infra-proximity distance rendering. CI (`.github/workflows/test.yml`) runs both on every PR. Don't ship a connector without tests — the schema's `extra="forbid"` will catch drift but tests catch logic regressions.
- **Map stack.** Leaflet + topojson-client (CDN, ~7KB) — no `leaflet.markercluster`. ~47k markers handled via Canvas renderer + decimation (1/8 at low zoom). Clustering not worth the payload — decimation is simpler and already keeps the visible count manageable.
- **Vector basemap (v1.5).** No tile provider. The map renders US states (`docs/data/us-states.json`, ~87KB) as filled GeoJSON polygons drawn on a Canvas in `tilePane`, with bold state strokes. US counties (`docs/data/us-counties-topo.json`, TopoJSON ~822KB) lazy-load via `ensureCountiesLoaded()` the first time the user zooms past `COUNTY_MIN_ZOOM` (7), are decoded to GeoJSON via `topojson.feature()`, and then attach/detach as the user crosses that zoom threshold. Outside CONUS the Leaflet container background = `--map-ocean`, so Canada/Mexico literally don't exist on the map. Adding the meta `<meta name="color-scheme" content="light dark">` is required so Chrome's auto-dark mode doesn't invert the canvas in production. **Don't** reintroduce raster tiles — the vector basemap is lighter, fully theme-able via CSS vars, and intentionally US-only. **Don't** layer a CSS `filter: brightness/contrast` on `.leaflet-tile-pane` to retheme — the filter recomposites every frame during pan/zoom and tanks mobile perf; instead, `refreshBasemapColors()` re-styles the GeoJSON layers + map bg from CSS vars on theme swap.
- **Vector basemap palette.** `--map-ocean` / `--map-land` / `--map-state-stroke` / `--map-county-stroke` / `--map-marker-stroke` are the single source of truth. JS reads them via `cssColor()` in `statesStyle()`, `countiesStyle()`, `addMarkersForRecords()`, and `refreshBasemapColors()`. Theme toggle calls `refreshBasemapColors()` after `applyTheme()` so the canvas redraws with new vars.
- **US-only constraint.** `maxBounds` (≈ `[18, -127]` to `[51, -65]`, continental US plus a strip below it for the inset boxes), `maxBoundsViscosity: 1.0`, and `minZoom: 4` together prevent the user from panning or zooming to non-US areas. Init uses `fitBounds(US_BOUNDS)` so the lower-48 fills the viewport. Don't loosen without a reason — the dashboard is a US tool and Canada/Mexico shouldn't dominate the basemap.
- **Cartographic insets for AK / HI / PR / VI / Pacific.** Real coordinates for those states/territories sit outside the lower-48 envelope, so `INSETS` in `app.js` linearly remaps each record's lat/lon into a labeled box at the bottom of the map (classic US-map-with-insets layout: ALASKA, HAWAII, PR / USVI, GU / MP / AS). `applyInsetRemap()` runs in `ingestSites` BEFORE markers are added; the original lat/lon is preserved on `lat_real`/`lon_real`. The detail panel (`d-coord`) and CSV export read `lat_real ?? lat` so users see real geographic coords. `drawInsetBoxes()` paints opaque rectangles (matching `--surface`) plus label divIcons; it's called once on init and again from the theme toggle so the rectangle fill repaints with the new theme. Adding a new state to an inset = one entry in `INSETS[].states`. Adding a brand new inset region = one entry in `INSETS` plus a label tag in CSS (`.inset-label` is the shared class).
- **Map legend.** Custom `L.control` in `bottomright` titled "Program", showing one row per program **present in the currently-loaded data** (first paint shows just Superfund; FUDS/BRAC/Brownfield rows appear as their lazy loads complete — `rerenderLegend()` is called from each lazy-load path and from the theme toggle). Colors come from CSS vars via `cssColor()`. Don't add NPL status rows back to the legend — that detail belongs in the table pill / detail panel.
- **Filter UX (v1.6).** Three pieces:
  - **NPL Status checkboxes.** Replaced `<select multiple>` (Cmd/Ctrl-click trap) with a fieldset of four checkboxes generated from `STATUS_LEGEND` into `#f-status-checks`. Delegated change handler → `populateStatusFilter()` can rebuild without re-binding. Reset clears all four.
  - **State dropdown with full names.** `STATE_NAMES` (50 + DC) and `TERRITORY_NAMES` (5 + FM) are the source of truth. Sorted by full name; territories grouped under `<optgroup label="Territories">` so AS doesn't sort before AZ. Anything not in either map falls into an "Other" optgroup so unknown codes still show up.
  - **Acreage slider tick labels.** `<datalist id="acreage-ticks">` provides browser-native marks; a sibling `.acreage-ticks-labels` row prints `1 / 10 / 100 / 1k / 10k / 100k / 1M` so users can read the log scale at a glance.
- **Accessibility (v1.6).** `<a class="skip-link" href="#main">` is the first focusable element (visually hidden until focused, then slides in at top:8px). `.topbar-controls` is `<nav aria-label="Toolbar">`; `<main id="main" tabindex="-1" role="main">` is the skip target; `<header role="banner">`. The detail panel's `aside` has `aria-hidden` synced to its `hidden` attr (`closeDetail()` and `selectSite()` both update it) plus `role="complementary"`.
- **Filtered summary.** When any filter is active (program toggle, state, status, acreage, search), `#search-count` shows `N of M [in <state>] · X ac [(K w/ acreage)]`. The acreage suffix sums `s.acreage` across the visible set (skipping nulls); the `K w/ acreage` parenthetical only appears when some visible sites lack acreage. This is the per-state summary surface — keep it lightweight; don't replace it with a separate panel.
- **Mobile detail panel.** Below 640px the side panel becomes a bottom sheet (`top:auto; bottom:0; border-radius` + drag-handle pseudo-element). Don't reach for a full-height slide-over on phones — it covers the map and breaks the "tap a marker, see info, keep panning" loop.
- **First-paint perf.** `index.html` preloads `data/sites.json` and `data/us-states.json` (`<link rel="preload" as="fetch" crossorigin>`). Leaflet, topojson-client, and `app.js` are `defer`-loaded so the JSON requests race them. Keep this pattern when adding new data files. Counties TopoJSON is intentionally **not** preloaded — it's lazy-fetched only when the user zooms in past `COUNTY_MIN_ZOOM`.
- **Touch hygiene.** `viewport-fit=cover`, `theme-color`, `-webkit-tap-highlight-color: transparent`, and `touch-action: manipulation` on interactive elements. Leaflet is initialized with `tap: false` to avoid duplicate touch+click events.
- **DOD FUDS connector (v1.7).** `connectors/dod_fuds.py` — USACE FeatureServer at `services7.arcgis.com/n1YM8pTrFmm7L4hs/arcgis/rest/services/fuds/FeatureServer/1`. ~10k point features, PAGE_SIZE=2000. CLI: `--fuds-state` (filter by state code), `--fuds-eligible-only` (skip Ineligible/Categorical Exclusion). Quirks: source returns lowercase state codes (uppercased in normalize), EPAREGION is a string like `"Region 1"` (parsed to int). ID namespace: `FUDS-<DODFUDSPROPERTYIDPK>`. Maps CURRENTOWNER→current_owner (one of the few sources that provides this). ~12.8% records drop (missing coordinates).
- **DOD BRAC connector (v1.7).** `connectors/dod_brac.py` — ESRI milbases FeatureServer at `services.arcgis.com/hRUr1F8lE8Jq2uJo/arcgis/rest/services/milbases/FeatureServer/0`, WHERE `BRAC_SITE='YES'` (27 features, polygon geometry). Computes acreage from polygon rings via Shoelace formula with cos(latitude) scaling (`polygon_area_sq_meters()` → acres). `envelope_center()` extracts centroid from the geometry envelope. ID namespace: `BRAC-<slugified SITE_NAME>`. Maps COMPONENT→component (military branch). Small dataset — no pagination needed.
- **EPA Redev enrichment connector (v1.7).** `connectors/epa_redev.py` — RedevelopmentAppSitePoints FeatureServer at `services.arcgis.com/cJ9YHowT8TU7DUyn/…/RedevelopmentAppSitePoints/FeatureServer/0`. 1,905 Superfund sites with infrastructure-proximity fields. PAGE_SIZE=1000 (server's maxRecordCount). Not a standalone program — `ensureRedevLoaded()` iterates its records and enriches matching Superfund records in `sitesById` by EPA_ID. `is_dc_candidate()`: requires `NearElectL` starts with "Yes" AND `acreage ≥ 50` AND `InWaterServiceArea` starts with "Yes". 828/1,905 flagged. **Don't** create markers for Redev records — they're enrichment-only.
- **Detail panel program branching (v1.7).** `selectSite()` in `app.js` conditionally shows/hides program-specific sections: `#d-fuds-block` (eligibility, fuds_status, has_projects) for FUDS, `#d-infra-block` fields (transmission, highway, railroad, water, wastewater, pop density, DC candidate) for Superfund records enriched by Redev. The generic owner section and sub-sites block remain cross-program. BRAC shows component in the status slot. When adding a new program, add a branch to `selectSite()` and conditionally show/hide the relevant detail blocks.
- **FUDS layer-1 + layer-4 join (v1.9).** `connectors/dod_fuds.py` now fetches BOTH layer 1 (~10k points, master list) and layer 4 (~3k polygons) from the FUDS FeatureServer and joins them by `DODFUDSPROPERTYIDPK`. Layer 4 contributes acreage (computed via `connectors.geom.polygon_acreage` — Shoelace + cos(lat); the source's `Shape__Area` is in degrees², not m²) and a polygon-centroid lat/lon (more accurate than the layer-1 point for properties spanning miles). ~3k previously-null FUDS acreages now populated. Same property can have multiple parcel-fragment polygons under one ID — `_fetch_polygon_join` concatenates rings into one entry before computing area, so we sum across fragments instead of taking the first. Toggle off the join with `--fuds-no-polygons` for fast iteration without the extra ~3k-feature pagination.
- **Polygon math lives in `connectors/geom.py` (v1.9).** Both BRAC and FUDS need the same Shoelace + cos(latitude) area calculation, so it's extracted to `polygon_area_sq_meters` + `polygon_acreage` + `envelope_center`. `dod_brac.py` re-exports them as static methods (`DodBrac.polygon_area_sq_meters`) for back-compat with `tests/test_brac.py`. `ACRES_PER_SQ_METER = 0.000247105` is the single conversion constant.
- **EPA Superfund Documents enrichment (v1.9).** `connectors/epa_superfund_docs.py` is enrichment-only — it reads `docs/data/superfund-npl.json`, walks three hops per site, writes `docs/data/epa-superfund-docs.json` containing `[{epa_id, documents: [...]}]`. The hops:
  1. **EPA pretty page → SF_SITE_ID.** EPA_ID and SF_SITE_ID are unrelated identifiers; no public cross-walk dataset exists. The connector fetches `s.profile_url` (which is `https://www.epa.gov/superfund/<slug>`) and regex-extracts `csitinfo.cfm?id=(\d+)` from the linked-out cumulis URL. Sites without a pretty URL (~0%) are skipped. **Don't** assume `EPA_ID` works on cumulis — it doesn't.
  2. **docdata HTML → collection IDs.** `cumulis.epa.gov/.../second.docdata?id=<SF_SITE_ID>` lists collections. Allowlist: "Key Documents", "SPP Decision Documents", "SPP Public Available Documents", "SPP Technical Reports and Studies", "SPP Enforcement and Settlement Documents". The "FY... AR" Administrative Record collections are intentionally skipped — high volume, low signal for buyers.
  3. **cachejson → docs.** `semspub.epa.gov/src/cachejson/<region>/<type>/<colid>` returns the JSON document index per collection. Records normalize to `{doc_id, title, url, date (ISO), category, ou_id?, size?, pages?}`. Sorted newest-first; deduped by `doc_id` (Key Documents often re-lists items from Decision Documents).
  Performance: ~3 HTTP requests per site (pretty page + docdata + ~5 cachejson calls). At 1.5s rate-limit, ~10s/site = ~5 hours unbounded. Default `--docs-limit 100 --docs-status F,D` (Final/Deleted NPL, sorted by acreage desc) keeps a single run ~15 min. `--docs-skip 100` stripes coverage; cache makes re-runs cheap. **Don't** raise `--docs-per-site` past ~10 — payload bloat with diminishing UX value.
- **`http_get_text` in `connectors.base` (v1.9).** Sister to `http_get_json` for HTML/XML scrapers. Same disk cache, same 1.5s delay, same User-Agent. Cache key uses `.txt` extension to avoid colliding with JSON cache for the same URL+params. Used by `epa_superfund_docs` for both the EPA pretty page and the cumulis docdata page. Caller catches `requests.HTTPError` and decides whether to skip-and-continue or abort — the function itself raises on any non-2xx so we fail loud on auth errors.
- **Owner provenance (v1.9).** Schema gains `current_owner_source: str | None`. Today only FUDS sets it (`"USACE FUDS"`); future ACRES PPF / Regrid / Landgrid integrations should set their own label. UI shows a small "Owner source" row below "Current owner" so users can cite. **Don't** put owner data on a record without a source — owner accuracy varies wildly across federal sources and a wrong owner on an unsourced field will be untraceable.
- **Documents block in detail panel (v1.9).** `selectSite()` calls `renderDocuments(s)`. The block is hidden when `s.documents` is empty. Each entry renders title (semspub link), size + page count, and a meta line (date · category). The block always shows an "All site documents on EPA →" deep-link to the cumulis docdata page so users can pivot to canonical EPA coverage even when our enrichment hasn't covered the site yet.
- **e2e selectSite hook (v1.9).** `window.__sites` and `window.__selectSite` are exposed for e2e tests so they can target an enriched record by id without depending on table sort order or marker decimation. `window.__sitesLoaded`, `window.__APP_READY__`, and `brownfield:ready` event remain the canonical readiness signals.
- **Universal infrastructure-proximity enrichment (v1.10).** `connectors/infra_proximity.py` is a third enrichment connector — it reads every per-program JSON file (`superfund-npl.json`, `epa-acres.json`, `dod-fuds.json`, `dod-brac.json`), fetches three public layers (HIFLD `Electric_Power_Transmission_Lines` ~52k polylines on `services1.arcgis.com/Hp6G80Pky0om7QvQ`, Census TIGERweb Primary Roads layer 2 with `MTFCC='S1100'` ~17.6k features, Census TIGERweb Railroads layer 9 ~111k features), and emits `docs/data/infra-proximity.json` with `[{id, program, transmission_mi, rail_mi, highway_mi}]`. Distances rounded to 0.1 mi; values >`MAX_DISTANCE_MI` (100 mi) are dropped (out-of-CONUS / Alaskan FUDS noise). Per-layer skip via `--infra-skip-{transmission,highway,rail}`. **Spatial index is pure-Python** — `connectors/spatial.py:SegmentIndex` uses a 0.25° lat/lon grid with ring-expansion + early-exit; no shapely/rtree dependency. ~1.8M segments fit in memory comfortably; 47k queries take ~90s/layer post-build. Frontend lazy-loads via `ensureInfraLoaded()` and joins onto `sitesById` by `id`. Detail panel renders the three new rows above the EPA RE-Powering qualitative ones (which only cover ~1.9k Superfund sites). **Don't** confuse these distances with the Redev qualitative `near_*` fields — they're complementary; one is mile-precision universal, the other is bucketed (\"Yes - 1 mile\") Superfund-only.
- **Connector run_order (v1.10).** `Connector.run_order` (default 100) lets `refresh.py --all` run enrichment connectors after their producers. `epa-superfund-docs` and `infra-proximity` set `run_order = 200/300` because they read `docs/data/superfund-npl.json` (and the other per-program files) — alphabetical-only ordering ran them BEFORE `superfund-npl` finished, leading to silent zero-row enrichments on cold runs. **Don't** add a new enrichment connector without setting `run_order > 100`.
- **TIGERweb page-size quirk (v1.10).** Census TIGERweb MapServer endpoints advertise `maxRecordCount: 100000`, but with `returnGeometry=true` the response payload limit kicks in much earlier — pages of 10k polylines return HTTP 500 ("Error performing query operation"). Use `page_size: 1000` for any TIGER polyline layer with geometry; HIFLD's FeatureServer caps cleanly at 2000.
- **Spatial grid index design (v1.10).** `connectors/spatial.py` is pure Python by choice — adding shapely+rtree would pull in C extensions and ~10MB of wheels for a static-data project. Trade-offs: bucket each polyline segment into every grid cell its bbox touches (cell size 0.25° ≈ 17 mi); per query, expand outward in Chebyshev rings and early-exit when the best distance is shorter than the inner edge of the next ring. Distance math is local equirectangular projection (cos(lat) at the query point), accurate to <1% in CONUS. Sentinel filter at insert time drops null-island (`(0,0)`) and out-of-range (`|lat|>90`, `|lon|>180`) vertices — both common in ESRI sources for "missing geometry" rows. **Don't** swap to a haversine-only nearest-vertex approximation — long polylines (cross-country transmission lines) have multi-mile gaps between vertices that would make nearest-vertex distance a useless overestimate.
- **Offline TIGER county fill (v1.10.1).** `connectors/county_lookup.py:CountyIndex.from_path()` decodes `docs/data/us-counties-topo.json` (the same TopoJSON the frontend lazy-loads for county polygons past zoom 7) into a 0.5°-cell spatial index of ~3,500 polygons, then point-in-polygon resolves any (lat, lon) to a county name in O(few-polygons-tested). `EpaAcres._fill_missing_county()` runs after normalize and reverse-geocodes the ~51% of ACRES records the source FeatureServer ships without `COUNTY` (~99.5% hit rate; the remaining ~0.3% are coastal points that fall just outside the polygon edge). Pure-Python; same trade-off as `connectors/spatial.py` — no shapely/rtree dependency. Disambiguates same-named counties across states by validating the polygon's FIPS-derived state against the record's `state` field. **Don't** add a per-record HTTP call to Census Geocoder — it's free but rate-limited and would turn a 5-second connector pass into a multi-hour batch job over 18k records.
- **FUDS owner-label normalization (v1.10.1).** USACE ships `current_owner` as raw concatenated codes (`"PRIV: PRIVATE   "`, `"FED: FEDERAL AIR FORCE  "`, `"LOCAL: CITY   ; PRIV: PRIVATE   "`, …). `connectors/dod_fuds.py:_pretty_owner()` runs in `normalize()` and maps the six known tier prefixes (PRIV/LOCAL/FED/STATE/TRIBE/OTHER) to clean labels — "Private", "Federal — Air Force", "Local government — City", etc. — collapsing redundant tier-word repetitions ("PRIV: PRIVATE PRIVATE" → "Private") and joining multi-tier semicolon-separated entries with " / ". Free-text owners without a known prefix are title-cased with an acronym whitelist (USFS, BLM, NPS, …) so agency abbreviations stay all-caps. The cache files keep the raw codes; only the emitted JSON has the cleaned values. Same pattern as the place-name prettifier, but connector-side rather than runtime — owner strings are too varied for a small `prettyPlace`-style heuristic.
- **Connector network resilience (v1.10.1).** Long enrichment runs (`epa-superfund-docs` with `--docs-limit 500` is ~75 min and ~3,000 HTTP requests) get clobbered by single-blip cumulis.epa.gov / semspub.epa.gov timeouts. `epa_superfund_docs.fetch_records()` now catches `requests.ConnectionError` / `requests.Timeout` per-site (and per-collection) and logs+continues, alongside the existing per-`HTTPError`-status handling. **Don't** raise these exceptions for transient network errors — a single connection blip would otherwise abort a multi-hour batch with no salvage of the work-in-progress. The `requests.HTTPError` paths still let non-transient codes through, so auth / 5xx that aren't in `TRANSIENT_HTTP_CODES` still fail loud.
- **FUDS detail-panel "Boundary not digitized" note (v1.10.1).** ~66% of FUDS records (5,832 / 8,822) lack acreage because USACE hasn't digitized layer-4 polygons for them — predominantly the Eligible tier, which is the highest-value redevelopment universe. The Acreage row in the detail panel renders an inline italic note ("Boundary not digitized in USACE source.") via `#d-acreage-note` when `s.program === "fuds" && s.acreage == null`, hidden otherwise. Note span sits inside the same `<dd id="d-acreage">` as a sibling text node — `selectSite()` rewrites only the leading text node so the span survives subsequent renders. Requires the `[hidden]` belt-and-suspenders rule (see "[hidden] trap" above) since the note is `display: inline`. **Don't** show this note for non-FUDS programs — ACRES brownfields also have universal `acreage: null` but the gap has a different cause (the ACRES FeatureServer just doesn't expose acreage at all; it's not a per-site digitization issue).
- **EPA ECHO enforcement enrichment (v1.11; two-hop fix in v1.11.2).** `connectors/epa_echo.py` is enrichment-only — reads `docs/data/superfund-npl.json` and emits `docs/data/epa-echo.json` with `[{id, program, epa_id, enforcement: {...}}]`. **Two HTTP calls per site** — this is the canonical ECHO REST flow: search returns a server-side QueryID; the actual rows download via `get_qid`:
  1. `echodata.epa.gov/echo/echo_rest_services.get_facilities?p_pid=<EPA_ID>` → returns a `QueryID` plus a row-count summary. **`p_pid` (program ID) is the right pivot, not `p_si`** — `p_si` is silently ignored by this endpoint and the response comes back as `{"Error": {"ErrorMessage": "Rows Returned would be 5619057. Queryset Limit would be exceeded - please make search parameters more selective."}}`. The v1.11 implementation used `p_si` and emitted **zero** records across the whole F/D universe before this was caught and fixed in v1.11.2 (the connector's existing tests passed because they were unit-mocked against a synthesized response shape rather than the live API).
  2. `echodata.epa.gov/echo/echo_rest_services.get_qid?qid=<QueryID>&qcolumns=...` → returns the actual `Facilities` row(s). The QueryID rotates every call, so **the `get_qid` cache key must NOT include the QueryID** — key by stable EPA_ID + qcolumns instead, otherwise no cache hits on re-run.
  - **qcolumns are column NUMBERS from `echo_rest_services.metadata`**: 1 (FAC_NAME), 4 (FAC_STATE), 6 (REGISTRY_ID), 36 (FAC_COMPLIANCE_STATUS), 41 (FAC_INSPECTION_COUNT, 5yr), 43 (FAC_DATE_LAST_INSPECTION), 49 (FAC_INFORMAL_COUNT, 5yr), 54 (FAC_FORMAL_ACTION_COUNT, 5yr), 55 (FAC_DATE_LAST_FORMAL_ACTION), 60 (FAC_TOTAL_PENALTIES), 61 (FAC_PENALTY_COUNT), 62 (FAC_DATE_LAST_PENALTY). The v1.11 column list (1,2,3,4,5,12,17,18,19,23,24,25,26,30) was wrong — those are FAC_NAME / FAC_STREET / FAC_CITY / FAC_STATE / FAC_ZIP / FAC_INDIAN_SPATIAL_FLG / FAC_LAT / FAC_LONG / AIR_IDS / CAA_PERMIT_TYPES, none of which are enforcement counters.
  - **`get_qid` ships dates as `MM/DD/YYYY`**, not ISO. `_safe_date()` normalizes to `YYYY-MM-DD` so frontend rendering is consistent across enrichment runs.
  - Defaults `--echo-limit 100 --echo-status F,D` so a single run is ~5 min (2 HTTP × 100 sites × 1.5s rate-limit); cache makes incremental runs cheap. Full F/D universe (1,787 sites) takes ~90 min unbounded.
  - The normalizer is **tolerant of multiple ECHO field names** across endpoints — `get_qid` returns `FacInspectionCount` / `FacFormalActionCount` / `FacInformalCount` / `FacTotalPenalties` / `FacComplianceStatus` / `FacDateLastInspection` / `FacDateLastFormalAction`, while older snapshots and the DFR endpoint use shorter aliases (`Insp5yr` / `FEA5yr` / `IEA5yr` / `Penalties` / `CurrentCompliance` / `LastInspection`). All accepted.
  - **Treat `0` as a meaningful signal**, not "no data" — `formal_actions_5yr=0` is a clean record, not "missing." `_safe_int` / `_safe_float` strip dashes (`--`), commas, and dollar signs.
  - Output dict carries `dfr_url` (deep link to the DFR page) for the detail-panel "Full report" link. `run_order = 250`. Frontend lazy-loads via `ensureEchoLoaded()`; `renderEnforcement()` highlights nonzero formal actions and nonzero penalties via the `.violation` class (red). Detail panel shows `last_violation_date`, `last_inspection_date`, and `last_formal_action_date` (added v1.11.2).
- **AI-summary enrichment (v1.11 / static mode v1.11.2 / prose rewrite v1.11.3).** `connectors/ai_summary.py` generates a one-paragraph plain-English summary per site for the detail-panel Summary tab. Two modes: (a) `--ai-static` — deterministic template from structured fields, no API key, runs in <1 s for all 1,787 sites; (b) default LLM mode — calls Claude Haiku per site, cached by content-hash. **Static mode is the default for bulk runs.** `_load_enriched_sites()` MERGES the per-program JSON with all four enrichment files (epa-redev, infra-proximity, epa-superfund-docs, epa-echo) so the prompt/template sees every signal on disk; `run_order = 400` (last). `_fingerprint(site)` hashes the fields that actually influence the narrative — lat/lon excluded so TIGER re-snaps don't invalidate the cache; document fingerprinting uses only `len(docs)` + sorted categories. **`_pretty_text()` title-cases raw ALL-CAPS strings.** Key design decisions: (1) state postal codes are in `_NAME_KEEP_UPPER` explicitly — the old `len(core) ≤ 2` catch-all was removed because it preserved prepositions (OF, IN) as uppercase; (2) `_title_word()` uses `re.sub(r"(?<![a-zA-Z'])([a-zA-Z])", ...)` not `str.title()` — the latter has two known bugs: apostrophe word-boundary (Beck'S) and leading-punctuation capitalization ((Rosemount) → (rosemount)); (3) `_pretty_token()` checks `not w_rstripped.endswith(".")` before treating a core as an acronym — "CO." (Company) strips to "CO" which is also Colorado's state code, and the period is the only reliable disambiguator; (4) article "a" vs "an" is computed from the first digit of the formatted acreage (only "8" → "an") or the first letter of the program label when no acreage. `build_static_summary()` suppresses the enforcement block when `formal_actions == 0 AND penalties == 0` — a clean ECHO compliance status is not a risk signal and shouldn't appear as enforcement content. **Prose rewrite (v1.11.3):** `build_static_summary()` now generates flowing sentences rather than a colon-separated field dump. Structure: lead sentence ("X is a Y-acre Program in City, ST, currently on the Final NPL.") → infrastructure ("Infrastructure proximity: transmission lines 0.8 miles from the boundary, rail 7.0 miles, and highway 31.9 miles." — Oxford-comma joined via `_join_list()`, "on-site" when distance < 0.05 mi via `_fmt_distance()`) → DC reuse ("The site meets EPA RE-Powering criteria as a data-center reuse candidate.") → owner → documents ("N federal documents on file (Category A, Category B, and Category C).") → enforcement ("EPA ECHO records show N formal enforcement actions in the past 5 years and $X in penalties.") → disclaimer ("AI-generated summary from federal records."). `_fmt_distance(v)` returns `"on-site"` when `v < 0.05` (avoids `"0.0 miles"`), otherwise `f"{v:.1f} miles"`. `_join_list(items)` returns Oxford-comma list: zero → `""`, one → item, two → `"A and B"`, three+ → `"A, B, and C"`. Sparse fallback (no infra/docs/enforcement data) emits a single natural sentence instead of an empty block. All summaries end with `"AI-generated summary from federal records."` so the generated nature is always attributed. Run: `python3 refresh.py --source ai-summary --ai-static --ai-status F,D --ai-limit 0`.
- **Detail-panel tab strip (v1.11).** The detail panel grew an Overview/Summary tab strip just under the title (`#dtab-overview`, `#dtab-summary`). Overview holds everything that was there before (KV grid, FUDS block, infra block, documents block, ECHO block); Summary is the AI card. `selectSite()` calls `resetDetailTabs()` so opening a new site always lands on Overview — a user reading a Summary on site A doesn't carry that pane state into site B. The panes use `[hidden]` + `display:none` (see "[hidden] trap"); `.dpane[hidden] { display: none }` is the explicit override. The summary card uses a 3px accent-colored left border (`border-left: 3px solid var(--accent)`) to visually distinguish AI-generated narrative from primary-source data — per the project's "source attribution" principle.
- **Readiness pills in the Program cell (v1.11.3).** `selectSite()` renders up to three optional outline pills next to the program pill, in this order: `Cleanup Complete` (`s.program === "superfund" && s.npl_status_code === "D"`), `Active Reuse` (`/^yes/i.test(s.in_reuse)` — RedevelopmentAppSitePoints `In_Reuse` flag, populated by `epa-redev`), and `DC candidate` (`s.data_center_reuse_candidate === true`). All three derive from data already on disk — zero new HTTP. Cleanup + Reuse share the green `--readiness-ready` token (light: `#2f7a4d`, dark: `#6cc18a`); DC candidate keeps the existing `--accent` outline so the three signals read as distinct ladder rungs. The `In_Reuse` source value is a string ("Yes"/"No"/blank), not a bool — match with `/^yes/i` so the regex doesn't accidentally hit the trailing "no" inside any longer value the source might ship.
- **Search index pre-built at ingest (v1.11.3).** `siteMatchesQuery()` previously joined `[name, city, county, state]` into a temporary string on every call — 47k allocations per keystroke. `ingestSites()` now writes `s._searchKey = [...].filter(Boolean).join(" ").toLowerCase()` once per record, and `siteMatchesQuery()` reads `s._searchKey.includes(q)`. The key is computed AFTER the `prettyName` / `prettyPlace` rewrites so the index reflects the rendered values, not the raw EPA strings. Don't fall back to a re-join when `_searchKey` is missing — that would silently defeat the optimization for any record added through a future ingest path that skipped the index. The intentional behavior is "no key → never matches" so the regression is loud.
- **Visible-bbox cached on `tableState` (v1.11.3).** `refitMapToFilters()` was sweeping all 47k records twice per filter toggle (one push into a lats/lons array, then `Math.min`/`Math.max` across each). `refreshTableForFilter()` now computes `tableState.visibleBBox = { minLat, maxLat, minLon, maxLon, count }` in the same loop that builds `tableState.filtered`, and `refitMapToFilters()` reads from the cache. Safe because every refit call site calls `applyFilter()` (which calls `refreshTableForFilter()`) first. Empty visible set → `visibleBBox = null`. Exposed on `window.__tableState` for e2e verification.
- **"Adjacent" replaces "0.0 mi" / "on-site" (v1.11.4).** The `infra-proximity` connector emits `round(d, 1)`, so any source distance under 0.05 mi lands as `0.0` — that's the "passes through / borders the boundary" case, not a missing value. Two surfaces render it: (a) frontend `fmt.miles()` returns `"Adjacent"` for `n < 0.05` (replacing the old `"<0.1 mi"` placeholder); (b) `connectors/ai_summary.py:_fmt_distance()` returns `"adjacent"` for the same threshold and `f"{v:.1f} miles away"` otherwise, with all three infra clauses (transmission / rail / highway) sharing the uniform `f"{noun} {_fmt_distance(v)}"` shape (the old code had a special `"on-site"` branch per-clause and a "from the boundary" suffix on transmission only — three different prose shapes for what is conceptually one signal). Threshold is 0.05 mi everywhere — picked because that's the connector's pre-rounding bin edge, so it's the cleanest possible drift-free guard. **Don't** introduce a different threshold in a third place (e.g. CSV export) without aligning all three; tests `test_fmt_distance_renders_adjacent_for_zero` + `test_static_summary_renders_adjacent_for_zero_distance` + `test_infra_distance_renders_adjacent_below_threshold` cover the contract end-to-end.
- **DC-candidate criteria sub-line in the detail panel (v1.11.4).** `data_center_reuse_candidate` is opaque on its own — "Yes" with no shown reasoning. The detail-panel `<dd id="d-dc-candidate">` carries an italicized `<span id="d-dc-criteria" class="dd-criteria">` sibling that reveals the EPA RE-Powering rule (`≥50 acres · electric transmission · water service area`) when the flag is true and stays `[hidden]` otherwise. Same text-node-replacement pattern as the FUDS `d-acreage-note` (rewrite `firstChild.nodeValue` only so the sibling span survives subsequent renders). The matching CSS class `.dd-criteria` is intentionally generalized (not `.dc-criteria`) so a future "why eligible?" sub-line on a different `<dd>` can reuse it. **Don't** drop the `[hidden]` belt-and-suspenders rule — see the broader "[hidden] trap" entry; `.dd-criteria` already has `display: block`, so the bare attribute won't override it. Same drift risk as `populateStatusFilter()` / Reset (UAT-007) — when the criteria definition itself changes (e.g. swap to mileage thresholds against `transmission_mi`), update the string in `app.js` *and* the KPI-deck subtext (`#kpi-dc-sub`) *and* the legend pill tooltip together.
- **DC-reuse parenthetical removed from static summaries (v1.11.4).** `build_static_summary()` previously emitted `"The site meets EPA RE-Powering criteria as a data-center reuse candidate (≥50 acres with electric transmission and water service access)."` for all 776 flagged sites — read as boilerplate at scale because the criteria never vary per site. Trimmed to `"Flagged as a data-center reuse candidate."`; the criteria now only appear in the three lower-noise surfaces (KPI subtext, legend tooltip, detail-panel `.dd-criteria`). **Don't** restore the parenthetical "for context" without a per-site signal that varies — repeating identical text across hundreds of summaries is the boilerplate failure mode AI summaries are meant to avoid.
- **Superfund acreage falls back to `polygon_acreage(rings)` (v1.11.5).** ~8% of NPL records (158 / 1,908) ship with `GIS_AREA_UNITS="Miles"` (a linear-feature flag — the `GIS_AREA` value is stream/road length in miles, not an area) or with both fields null. Pre-v1.11.5 those records emitted `acreage: null` and dropped to the bottom of the sorted table. `SuperfundNPL.normalize()` now computes acreage from the polygon rings via `connectors.geom.polygon_acreage()` (the same Shoelace + cos(latitude) helper FUDS / BRAC use) when units aren't `"Acres"` / `"Square Miles"`. Zero new HTTP calls — the rings are already in every feature payload. Recovers values like Aerojet General Corp (20,874 ac), Alameda NAS (840 ac), Aberdeen Pesticide Dumps (57 ac); these were all "we have the polygon, just chose not to compute" gaps. Tests in `tests/test_normalize.py` lock in the fallback for the three trigger conditions (units=Miles, units=null, GIS_AREA=null with units=Acres). **Don't** restore the prior "Miles → null" behavior — the unit string is a *source-side hint about what GIS_AREA means*, not a claim that the geometry doesn't have area; trusting it once cost us 8% of the NPL.
- **Infra-proximity emits tombstone records when all layers are out-of-range (v1.11.5).** Pre-v1.11.5, `InfraProximity.fetch_records()` had `if not any_field: continue` — sites where every layer returned `None` (segment not in source — HIFLD transmission is CONUS-only, off-grid Alaska / Pacific simply isn't in the dataset) or `> MAX_DISTANCE_MI` were silently dropped from `infra-proximity.json`. That hid 542 sites (≈ 4 superfund + 142 ACRES + 395 FUDS + 1 BRAC, almost all in AK / Northern Mariana / Federated States / American Samoa) and made the file's `id` set ambiguous: a missing record could mean "enrichment never ran for this id" or "no infra in reach." The connector now always emits `{id, program}` even with no distance fields, so the join file is the source of truth for "we tried" and the field-absence signal stays clean for "no value within 100 mi or no source coverage." Frontend behavior unchanged — `setMileCell()` already renders `value == null` as "Not available" / "Remote — outside continental US" via the `_inset` flag. **Don't** raise `MAX_DISTANCE_MI` to "fix" the AK gap by reporting 600-mile distances — verified: the source itself returns `None` for these sites (HIFLD has no off-grid AK transmission, TIGER S1100 / rail are sparse off the Alaska Railroad corridor); raising the cap doesn't surface real values, it just lets noise through for the rare in-CONUS sites that happen to sit between coverage zones.
- **ACRES + FUDS acreage gaps are dead-ends (v1.11.5).** Two large acreage gaps remain by design after the v1.11.5 backfill — both blocked by source-side limits, not connector bugs:
  1. **EPA ACRES (36,003 / 36,003 missing, 100%).** The `All ACRES Properties` FeatureServer has no acreage column at all (`fields = [PROPERTY_ID, PROPERTY_NAME, RS_REGION_ID_1000, LABEL_STATE, ADDRESS1, CITY, COUNTY, ZIP_CODE, LATITUDE_MEASURE, LONGITUDE_MEASURE, ObjectId]` — point geometry only, no polygons to compute from). The per-property `acres6.epa.gov/.../PropertyProfileReports/Output/<id>.html` pages used to be public but now redirect to the EPA Login Gateway, so HTML scraping is also blocked. Frontend already documents this gap implicitly via the universal "—" rendering for ACRES acreage; no detail-panel note like FUDS gets because the cause isn't per-site digitization, it's a structural source limitation.
  2. **DOD FUDS (5,832 / 8,822 missing, 66.1%).** Layer 4 (`FUDS Property Polygon`) is the only USACE source with property boundaries; ~6k properties simply haven't been digitized there. Layers 3 (`Munitions Response Site`) and 7 (`IRM Project Boundary`) DO have polygons + `Shape__Area` and join keys (`FUDSPROPERTYID` / `FUDS_NUMBER` — both match layer 1's `DODFUDSPROPERTYIDPK`), but they cover SUBSETS of properties (the cleanup zone, not the property), so summing them would systematically *understate* property size. The "Boundary not digitized in USACE source" detail-panel note (v1.10.1) is the right disclosure. **Don't** join layer 3 or 7 areas into `acreage` — past investigations confirmed the geometric subset relationship; doing so would silently misreport acreage on the very records most likely to be evaluated for redevelopment.
