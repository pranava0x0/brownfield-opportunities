# Brownfield Opportunities — US contaminated-site atlas for data-center & power-generation siting

> An open dataset and interactive map of ~46,800 federally-tracked contaminated and formerly-used US sites (EPA Superfund/NPL, EPA ACRES brownfields, DOD FUDS, DOD BRAC), enriched with grid, gas, flood, opportunity-zone, climate, and tax-incentive data, and scored 0–100 for two siting use cases: hosting a data-center electrical LOAD, and building new power GENERATION.

The dashboard is a static, dependency-light site (Leaflet + vanilla JS — no backend, no build step). Every data file is open and fetchable as JSON from `data/`. The two suitability scores are computed client-side in `dc-score.js` as pure, deterministic functions of signals already on each record, so any consumer can reproduce or re-weight them. All source data derives from public US federal datasets (EPA, USACE, DOD, HIFLD, Census TIGER, FEMA, HUD, EIA); per-source attribution is in the dashboard footer and `schema.py`.

## Site

- [Interactive dashboard](https://pranava0x0.github.io/brownfield-opportunities/): map + sortable table, program/state/acreage/NPL/persona filters, and a per-site detail panel with the suitability scores, infrastructure proximity, enforcement, documents, and an AI summary. URL state (`?site=`, `?q=`, `?state=`, `?status=`, `?program=`, `?dc_tier=`, `?oz=`) is shareable.
- [Source repository](https://github.com/pranava0x0/brownfield-opportunities): connectors, schema, tests, and refresh tooling.

## Data files (open JSON, served from data/)

- [sites.json](https://pranava0x0.github.io/brownfield-opportunities/data/sites.json): Superfund/NPL sites, the eager first-paint set (~1,900 records). Mirror of superfund-npl.json.
- [epa-acres.json](https://pranava0x0.github.io/brownfield-opportunities/data/epa-acres.json): ~36,000 EPA ACRES brownfield properties (lazy-loaded).
- [dod-fuds.json](https://pranava0x0.github.io/brownfield-opportunities/data/dod-fuds.json): ~8,800 DOD Formerly Used Defense Sites.
- [dod-brac.json](https://pranava0x0.github.io/brownfield-opportunities/data/dod-brac.json): 27 DOD BRAC base-closure sites.
- [infra-proximity.json](https://pranava0x0.github.io/brownfield-opportunities/data/infra-proximity.json): per-site distances (miles) to transmission lines (+kV), substations (+kV), power plants (+MW & fuel), gas pipelines, rail, and highways, plus the FEMA flood-zone code and Special-Flood-Hazard-Area flag. Joined onto every program by `id`.
- [opportunity-zone.json](https://pranava0x0.github.io/brownfield-opportunities/data/opportunity-zone.json): Treasury Qualified Opportunity Zone containment (tract GEOID + rural flag).
- [ira-energy-community.json](https://pranava0x0.github.io/brownfield-opportunities/data/ira-energy-community.json): IRA (Inflation Reduction Act) energy-community designation per site — `in_energy_community` (bool), `energy_community_type` (`coal_closure` | `fossil_fuel_employment`), `energy_community_detail` (reason). Clean-energy projects on an energy community earn a +10 percentage-point ITC/PTC bonus under IRA §45/48. ~31.6% of sites qualify. Source: DOE NETL 2024 coal-closure tract + fossil-fuel-employment statistical-area layers (point-in-polygon).
- [iso-rto.json](https://pranava0x0.github.io/brownfield-opportunities/data/iso-rto.json): ISO/RTO market (PJM, MISO, ERCOT, CAISO, SPP, NYISO, ISO-NE, or non-RTO).
- [climate-zone.json](https://pranava0x0.github.io/brownfield-opportunities/data/climate-zone.json): IECC/ASHRAE county climate zone.
- [fema-nri.json](https://pranava0x0.github.io/brownfield-opportunities/data/fema-nri.json): FEMA National Risk Index per site (county-level) — `nri_risk_score` (0-100 composite), `nri_risk_rating`, and per-hazard ratings for the three DC-relevant hazards: `nri_wildfire_rating`, `nri_drought_rating`, `nri_heatwave_rating`. Wildfire/drought at Relatively-High or Very-High apply a climate penalty (−5 / −10) to both siting scores. 35,783 / 46,760 sites matched (CONUS counties). Source: FEMA NRI county FeatureServer (point-in-polygon).
- [epa-redev.json](https://pranava0x0.github.io/brownfield-opportunities/data/epa-redev.json): EPA RE-Powering infrastructure-proximity flags + the data-center-reuse-candidate boolean (Superfund-only enrichment).
- [epa-echo.json](https://pranava0x0.github.io/brownfield-opportunities/data/epa-echo.json): EPA ECHO enforcement & compliance summary (Superfund-only).
- [eia-retired-plants.json](https://pranava0x0.github.io/brownfield-opportunities/data/eia-retired-plants.json): distance to the nearest large (≥100 MW) retired dispatchable power plant from EIA-860M (7,620 sites within 5 mi). Fields: `retired_plant_mi`, `retired_plant_mw`, `retired_plant_fuel`, `retired_plant_year`, `retired_plant_name`. Sites within 1 mi trigger the "Ret. Plant" badge and grid-inheritance scoring credit (the Conesville/Widows Creek pattern: inherited transmission + stranded interconnect agreement).
- [reference-campuses.json](https://pranava0x0.github.io/brownfield-opportunities/data/reference-campuses.json): curated list of 10 confirmed hyperscale and colocation data center projects on brownfield / former industrial land (Google/Widows Creek coal plant AL, Aligned/Conesville coal plant OH, Homer City PA gas+DC campus, AWS/Susquehanna nuclear PA, Keystone Fairless Works steel mill PA, Project Marvel AL, Meta/Forest City textile mill NC, QTS/Qimonda fab VA, Cheswick coal plant PA, Microsoft/Foxconn site WI). Rendered as gold ★ overlay markers on the map; static JSON refreshed annually.
- [retired-industrial.json](https://pranava0x0.github.io/brownfield-opportunities/data/retired-industrial.json): overlay of ~660 large RETIRED industrial sites nationwide that ceased EPA GHGRP reporting — ~530 manufacturing (NAICS 31–33: smelters, steel/paper mills, cement, glass, chemical plants, refineries, auto-assembly) + ~125 mining (NAICS 212, mostly coal mines/prep plants). Candidate data-center sites because a closed smelter/mill/refinery/mine leaves a large stranded grid interconnection. Fields: `id`, `name`, `lat`, `lon`, `state`, `sector`, `category` (Manufacturing|Mining), `naics`, `last_report_year`, `reporting_status`, `parent_company`. Rendered as rust ◆ overlay markers. Generated by `scripts/build_retired_industrial.py` (queries GHGRP by `reporting_status`); refresh after each annual GHGRP release.
- [planned-retirements.json](https://pranava0x0.github.io/brownfield-opportunities/data/planned-retirements.json): overlay of ~90 OPERATING power plants ≥100 MW (75+ GW total) with ANNOUNCED retirement dates from the EIA-860M "Operating" sheet — a dated, forward-looking hand-off of a large grid interconnection (the Homer City pattern: repowering/co-location deals are struck before shutdown). Fields: `id`, `name`, `lat`, `lon`, `state`, `county`, `mw`, `units_retiring`, `fuel` (coal|gas|oil|nuclear), `first_retirement_year`, `last_retirement_year`, `balancing_authority`, `operator`. Rendered as teal ⬢ overlay markers. Generated by `scripts/build_planned_retirements.py`; refresh alongside EIA-860M releases.
- [ap1000-sites.json](https://pranava0x0.github.io/brownfield-opportunities/data/ap1000-sites.json): curated AP1000 nuclear-reactor siting overlay — 14 named US military installations screened for hosting a Westinghouse AP1000 (1,117 MWe). Transmission/substation/gas/rail/highway are joined from infra-proximity.json; developable acreage, cooling water, fiber, and construction-workforce availability are analyst-researched, each with a per-field source URL. Source-program split: 9 sites are on the US Army "Janus" microreactor shortlist (Nov 2025), 5 are from the US Air Force AI-data-center Request for Lease (solicitation AFCEC-26-R-0002). A separate overlay (not part of the ~46,800-site brownfield corpus); rendered in the dashboard's AP1000 tab, not on the map. Generated by `scripts/build_ap1000_sites.py` (hand-curated; refreshed manually). Per-site water claims are validated with USGS streamgages + the Vogtle 3&4 environmental record in `ap1000-water-validation.md`.
- [epa-superfund-docs.json](https://pranava0x0.github.io/brownfield-opportunities/data/epa-superfund-docs.json): curated federal documents per Superfund site (RODs, Five-Year Reviews, fact sheets).
- [ai-summary.json](https://pranava0x0.github.io/brownfield-opportunities/data/ai-summary.json): one-paragraph plain-English site summaries synthesized from the structured fields.

## Scoring & schema

- [dc-score.js](https://github.com/pranava0x0/brownfield-opportunities/blob/main/docs/dc-score.js): the two siting lenses. `computeDcCompositeScore(site)` scores a data-center LOAD (weights power access — transmission distance/voltage/substation/power-plant co-location — plus acreage, gas, logistics, readiness); `computeGenerationScore(site)` scores NEW GENERATION (land-dominant, transmission+substation to export, gas feedstock, ISO/RTO market). Both 0–100, both gate on transmission data (null = "can't assess"), both subtract an 18-point penalty for a FEMA Special Flood Hazard Area and a 5/10-point climate penalty for FEMA NRI Relatively-High / Very-High wildfire or drought risk. Each has a `*Breakdown()` companion returning per-component points. Grid-inheritance scoring (v1.20): every plant pathway scales by MW (100 MW → 50%, ≥1 GW → 100%); retired dispatchable plants score ≤1 mi full / 1–3 mi half, decayed by retirement recency (≥2018 full, 2008–2017 ×0.75, older ×0.5); active large coal/gas ≤1 mi full, 1–5 mi at 30%; active nuclear ≥500 MW ≤5 mi at 45% (AWS/Susquehanna PPA pattern). The generation lens carries a parallel `grid_reuse` component (retired-plant interconnection repowering — coal-to-solar/storage/gas) and a longer gen-tie transmission reach (tapers to 10 mi vs the DC lens's 2). Continuous signals (distance/acreage/MW) interpolate piecewise-linearly between documented anchors instead of stepping; substations provably distribution-class (`substation_kv` < 69 from the OSM voltage tag) are discounted, nulls unpunished. Readiness scoring (v1.18): IRA energy-community sites earn +3 readiness (DC lens) / +3 (generation lens, > OZ's +2) — the +10pp ITC/PTC bonus matters most to a ground-up generation build.
- [ap1000-score.js](https://github.com/pranava0x0/brownfield-opportunities/blob/main/docs/ap1000-score.js): a third, separate lens scoring only the AP1000 overlay (above) — `computeAp1000Score(site)` / `computeAp1000Breakdown(site)`, 0–100, weights summing to 100: cooling water 40, transmission 22, substation 16, construction workforce 15, fiber 7. Developable acreage is an eligibility THRESHOLD (≥500 acres), not a ranked factor. Water uses a 5-tier adequacy scale (abundant / adequate / marginal / poor / severe); "severe" (desert basins in overdraft, requiring dry-cooling) scores 0. A single AP1000 consumes ~20 MGD (Vogtle 3&4 NRC/DOE environmental record); per-site water adequacy is validated against USGS streamgages in `ap1000-water-validation.md`.
- [schema.py](https://github.com/pranava0x0/brownfield-opportunities/blob/main/schema.py): the `SiteRecord` Pydantic schema — the single source of truth for every field on every record. Each record sets `id` (cross-source unique) and `program` (`superfund` | `brownfield` | `fuds` | `brac`). (The AP1000 overlay is curated separately and is not a `SiteRecord`.)

## Notes

- Distances are in miles; values over 100 mi are dropped as out-of-CONUS. A distance of `0.0` means "adjacent" (borders the boundary), not missing.
- Acreage is unavailable for all ACRES brownfields and ~66% of FUDS sites (source-side gaps, not omissions) — those fall to the bottom of acreage sorts and render "—".
- Flood-zone coverage is partial and being backfilled (per-site FEMA queries); a missing `in_sfha` means "not yet checked / unmapped", not "no risk", and carries no score penalty.
- Data refreshes on a cron via GitHub Actions; the dashboard footer shows the refresh date.
